---
context: phase
phase: 37-production-ready-baseline
task: null
total_tasks: 4
status: in_progress
workflow: discuss-phase
last_updated: 2026-04-17T08:17:45.453Z
---

# Resume: Phase 37 Discuss — Gray-Area Selection

<current_state>
Paused mid-`/gsd-discuss-phase 37`. Pre-work complete: loaded PROJECT.md, STATE.md,
ROADMAP.md, all 4 phase plans, FINDINGS.md (F-01..F-19), SEED-001, SEED-002.
Identified 7 gray areas worth validating before execution. User was presented with
4 of them via AskUserQuestion and chose to pause instead of picking.

No plans have been modified. No execution started.
</current_state>

## Critical Anti-Patterns

_None discovered this session. The existing Phase 37 plans are reviewed but not
yet edited; no anti-patterns surfaced during analysis._

## Required Reading (in order)

1. `.planning/phases/37-production-ready-baseline/37-01-PLAN.md` through `37-04-PLAN.md` — the 4 plans being reviewed
2. `.planning/sessions/2026-04-17-baseline-test/FINDINGS.md` — F-01..F-19, the source of every plan decision
3. `.planning/seeds/SEED-001-strict-mode-deserialize.md` — strict mode spec
4. `.planning/seeds/SEED-002-sql-identifier-whitelist.md` — identifier validator spec
5. `.planning/HANDOFF.json` — `gray_areas_identified` is the full list of 7 to present (not just the 4 from last session's AskUserQuestion)

## Infrastructure State

- **Swift 2.2 host**: RUNNING on https://localhost:54035 (bash task `bbzr2zboc`).
  Config = baseline (original preserved at `Serializer.config.json.pre-baseline-test`).
  Admin: Administrator / Administrator1. User was reviewing baseline behavior here.
- **CleanDB host**: stopped.
- **Git**: clean. Last commit `3ded157` (session summary for baseline test).

<completed_work>

This session:
- Loaded Phase 37 context (plans, findings, seeds, memory)
- Identified 7 gray areas where plan authors made choices worth validating
- Started Swift 2.2 host on 54035 at user's request so they could review the baseline
- User declined to pick areas — wanted to save state and resume later

No code or planning artifacts modified.
</completed_work>

<remaining_work>

1. Resume `/gsd-discuss-phase 37` — present all 7 gray areas (see HANDOFF.json `gray_areas_identified`)
2. Deep-dive selected areas — user picks, Claude asks clarifying questions per workflow
3. Write CONTEXT.md with decisions
4. `/gsd-plan-phase 37` — replan to incorporate decisions (existing plans may need revision)
5. `/gsd-execute-phase 37` — run the 4 plans

</remaining_work>

<decisions_made>

This session:
- Leave Swift 2.2 `Serializer.config.json` as the baseline config (don't restore original). Original is preserved at `Serializer.config.json.pre-baseline-test`.
- Route phase 37 via `/gsd-discuss-phase` before execution (user wants to review, not accept plans as-is).

From prior sessions (relevant to Phase 37):
- No backward compatibility needed — beta product, 0.x. Memory: `feedback_no_backcompat.md`.
  → Implication for Plan 37-01: default `deserializeMode` does NOT need to be `SourceWins` for backcompat. Worth challenging.

</decisions_made>

<blockers>

- User input needed on gray-area selection before CONTEXT.md can be written.

</blockers>

## 7 Gray Areas to Present on Resume

(Full detail in HANDOFF.json `gray_areas_identified`. Short list:)

1. **Seed mode shape + default** — per-predicate enum (plan's choice) vs top-level `seed:` section vs hash idempotence. Default `SourceWins` vs `IfAbsent` for Content.
2. **Registry overlap** — `ShopIndexRepository`/`ShopIndexName` are in BOTH `RuntimeColumnsRegistry` (37-02) and `CredentialColumnsRegistry` (37-03). Needs single source of truth.
3. **Stale cleanup strategy** — atomic dir swap (plan's choice) vs files-written manifest.
4. **Plan 37-04 scope** — 5 subsystems bundled; split diff/template/strict into own plans or keep?
5. **Strict mode default** — opt-in (SEED-001 spec) vs default-ON for API path.
6. **Template asset scope cap** — manifest-only (plan's choice) vs serialize templates too.
7. **Cross-env link resolution for SqlTable** — LINK-02 approach. Perf of scanning every varchar on write.

<context>
Plans were authored same day (2026-04-17) as the baseline test, drawing directly
from FINDINGS F-01..F-19. They reflect my synthesis of the findings, not an
explicit user sign-off on the shape. User's "review everything with gsd discuss"
is the validation step — the plans should be treated as drafts until CONTEXT.md
captures their decisions.

The biggest structural question is (1) seed mode default. If we flip to IfAbsent
for Content by default, existing Swift 2.2 baselines keep working (they re-deploy
only absent pages on re-run) AND live prod is safe by default. Plan 37-01's
"default to SourceWins for backcompat" logic dissolves under the no-backcompat
memory.

Also: the running Swift 2.2 host will consume memory/CPU while paused. Offer to
stop it on resume if user is truly closing the session.
</context>

<next_action>
On `/gsd-resume-work`:
1. Detect this HANDOFF.json
2. Route to `/gsd-discuss-phase 37`
3. Skip the pre-work (already done — in HANDOFF.json)
4. Present all 7 gray areas from `gray_areas_identified` via AskUserQuestion (multiSelect)
5. Ask user whether to stop the Swift 2.2 host
</next_action>
