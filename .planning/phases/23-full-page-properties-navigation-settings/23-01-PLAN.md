---
phase: 23-full-page-properties-navigation-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/DynamicWeb.Serializer/Models/SerializedPage.cs
  - src/DynamicWeb.Serializer/Models/SerializedSeoSettings.cs
  - src/DynamicWeb.Serializer/Models/SerializedUrlSettings.cs
  - src/DynamicWeb.Serializer/Models/SerializedVisibilitySettings.cs
  - src/DynamicWeb.Serializer/Models/SerializedNavigationSettings.cs
  - src/DynamicWeb.Serializer/Serialization/ContentMapper.cs
  - tests/DynamicWeb.Serializer.Tests/Models/DtoTests.cs
  - tests/DynamicWeb.Serializer.Tests/Infrastructure/YamlRoundTripTests.cs
autonomous: true
requirements:
  - PAGE-01
  - ECOM-01

must_haves:
  truths:
    - "SerializedPage carries all ~30 missing properties with correct types and defaults"
    - "4 booleans (Allowclick, Allowsearch, ShowInSitemap, ShowInLegend) default to true on the DTO"
    - "ActiveFrom/ActiveTo are nullable DateTime on the DTO"
    - "ContentMapper.MapPage() populates all new properties from DW Page object"
    - "NavigationSettings is only serialized when UseEcomGroups is true"
    - "Sub-objects (Seo, UrlSettings, Visibility, NavigationSettings) round-trip through YAML"
    - "Old YAML without new properties deserializes with correct boolean defaults"
  artifacts:
    - path: "src/DynamicWeb.Serializer/Models/SerializedSeoSettings.cs"
      provides: "SEO sub-record with 7 properties"
    - path: "src/DynamicWeb.Serializer/Models/SerializedUrlSettings.cs"
      provides: "URL settings sub-record with 4 properties"
    - path: "src/DynamicWeb.Serializer/Models/SerializedVisibilitySettings.cs"
      provides: "Visibility sub-record with 3 properties"
    - path: "src/DynamicWeb.Serializer/Models/SerializedNavigationSettings.cs"
      provides: "Navigation settings sub-record with 8 properties"
    - path: "src/DynamicWeb.Serializer/Models/SerializedPage.cs"
      provides: "Extended with ~15 flat properties + 4 sub-object references"
    - path: "src/DynamicWeb.Serializer/Serialization/ContentMapper.cs"
      provides: "MapPage extended + MapNavigationSettings helper"
  key_links:
    - from: "src/DynamicWeb.Serializer/Serialization/ContentMapper.cs"
      to: "src/DynamicWeb.Serializer/Models/SerializedPage.cs"
      via: "MapPage return expression"
      pattern: "NavigationTag.*ShortCut.*Seo.*UrlSettings.*Visibility.*NavigationSettings"
---

<objective>
Extend the SerializedPage DTO with all ~30 missing page properties (organized into 4 sub-objects + flat scalars) and extend ContentMapper.MapPage() to populate them from DW Page objects. Includes unit tests for DTO defaults and YAML round-trip.

Purpose: The serialization side of PAGE-01 and ECOM-01 -- pages serialized from source produce YAML containing all properties.
Output: 5 new/modified model files, extended ContentMapper, extended test files.
</objective>

<execution_context>
@C:\Users\justi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\justi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-full-page-properties-navigation-settings/23-RESEARCH.md
@src/DynamicWeb.Serializer/Models/SerializedPage.cs
@src/DynamicWeb.Serializer/Serialization/ContentMapper.cs
@tests/DynamicWeb.Serializer.Tests/Models/DtoTests.cs
@tests/DynamicWeb.Serializer.Tests/Infrastructure/YamlRoundTripTests.cs
@tests/DynamicWeb.Serializer.Tests/Fixtures/ContentTreeBuilder.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create sub-record DTOs and extend SerializedPage</name>
  <files>
    src/DynamicWeb.Serializer/Models/SerializedSeoSettings.cs,
    src/DynamicWeb.Serializer/Models/SerializedUrlSettings.cs,
    src/DynamicWeb.Serializer/Models/SerializedVisibilitySettings.cs,
    src/DynamicWeb.Serializer/Models/SerializedNavigationSettings.cs,
    src/DynamicWeb.Serializer/Models/SerializedPage.cs,
    tests/DynamicWeb.Serializer.Tests/Models/DtoTests.cs,
    tests/DynamicWeb.Serializer.Tests/Infrastructure/YamlRoundTripTests.cs,
    tests/DynamicWeb.Serializer.Tests/Fixtures/ContentTreeBuilder.cs
  </files>
  <behavior>
    - Test: SerializedPage boolean defaults -- new page with only required fields has Allowclick=true, Allowsearch=true, ShowInSitemap=true, ShowInLegend=true, Hidden=false
    - Test: SerializedPage ActiveFrom/ActiveTo default to null
    - Test: SerializedPage Seo/UrlSettings/Visibility/NavigationSettings default to null
    - Test: YAML round-trip of page with all new flat properties (NavigationTag, ShortCut, Hidden, Allowclick, Allowsearch, ShowInSitemap, ShowInLegend, SslMode, ColorSchemeId, ExactUrl, ContentType, TopImage, DisplayMode, ActiveFrom, ActiveTo)
    - Test: YAML round-trip of page with Seo sub-object (MetaTitle, MetaCanonical, Description, Keywords, Noindex, Nofollow, Robots404)
    - Test: YAML round-trip of page with UrlSettings sub-object (UrlDataProviderTypeName, UrlDataProviderParameters, UrlIgnoreForChildren, UrlUseAsWritten)
    - Test: YAML round-trip of page with Visibility sub-object (HideForPhones, HideForTablets, HideForDesktops)
    - Test: YAML round-trip of page with NavigationSettings sub-object (UseEcomGroups=true, ParentType="Groups", Groups="GROUP1", ShopID="SHOP1", MaxLevels=3, ProductPage="Default.aspx?Id=42", NavigationProvider="EcomNavProvider", IncludeProducts=true)
    - Test: Backward compat -- old YAML without new properties deserializes correctly (Allowclick=true, Allowsearch=true, ShowInSitemap=true, ShowInLegend=true, ActiveFrom=null, ActiveTo=null, sub-objects=null)
  </behavior>
  <action>
    **Create 4 new sub-record files in Models/ (per PAGE-01, ECOM-01):**

    1. `SerializedSeoSettings.cs` -- namespace `DynamicWeb.Serializer.Models`, public record with:
       - `string? MetaTitle { get; init; }`
       - `string? MetaCanonical { get; init; }`
       - `string? Description { get; init; }`
       - `string? Keywords { get; init; }`
       - `bool Noindex { get; init; }` (default false)
       - `bool Nofollow { get; init; }` (default false)
       - `bool Robots404 { get; init; }` (default false)

    2. `SerializedUrlSettings.cs` -- public record with:
       - `string? UrlDataProviderTypeName { get; init; }`
       - `string? UrlDataProviderParameters { get; init; }`
       - `bool UrlIgnoreForChildren { get; init; }` (default false)
       - `bool UrlUseAsWritten { get; init; }` (default false)

    3. `SerializedVisibilitySettings.cs` -- public record with:
       - `bool HideForPhones { get; init; }` (default false)
       - `bool HideForTablets { get; init; }` (default false)
       - `bool HideForDesktops { get; init; }` (default false)

    4. `SerializedNavigationSettings.cs` -- public record with:
       - `bool UseEcomGroups { get; init; }` (default false)
       - `string? ParentType { get; init; }` ("Groups" or "Shop" as string)
       - `string? Groups { get; init; }`
       - `string? ShopID { get; init; }`
       - `int MaxLevels { get; init; }` (default 0)
       - `string? ProductPage { get; init; }` (stores Default.aspx?Id=NNN -- link resolved in plan 02)
       - `string? NavigationProvider { get; init; }`
       - `bool IncludeProducts { get; init; }` (default false)

    **Extend SerializedPage.cs** -- add after existing properties, before Fields:

    Flat scalar properties:
    - `string? NavigationTag { get; init; }`
    - `string? ShortCut { get; init; }` (stores Default.aspx?ID=NNN -- resolved in plan 02)
    - `bool Hidden { get; init; }` (default false)
    - `bool Allowclick { get; init; } = true;` (CRITICAL: DW default is true)
    - `bool Allowsearch { get; init; } = true;` (CRITICAL: DW default is true)
    - `bool ShowInSitemap { get; init; } = true;` (CRITICAL: DW default is true)
    - `bool ShowInLegend { get; init; } = true;` (CRITICAL: DW default is true)
    - `int SslMode { get; init; }`
    - `string? ColorSchemeId { get; init; }`
    - `string? ExactUrl { get; init; }`
    - `string? ContentType { get; init; }`
    - `string? TopImage { get; init; }`
    - `string? DisplayMode { get; init; }` ("Normal" or "List" as string)
    - `DateTime? ActiveFrom { get; init; }` (nullable to distinguish "not set")
    - `DateTime? ActiveTo { get; init; }` (nullable to distinguish "not set")
    - `int PermissionType { get; init; }`

    Sub-object references:
    - `SerializedSeoSettings? Seo { get; init; }`
    - `SerializedUrlSettings? UrlSettings { get; init; }`
    - `SerializedVisibilitySettings? Visibility { get; init; }`
    - `SerializedNavigationSettings? NavigationSettings { get; init; }`

    DO NOT add ShowInMenu, Published (computed read-only), ShowUpdateDate (obsolete), MasterPageId, MasterType, Level, IsMaster, IsLanguage.

    **Extend tests:**

    In `DtoTests.cs`, add tests for boolean defaults, nullable DateTime defaults, and null sub-objects.

    In `YamlRoundTripTests.cs`, add tests for:
    - Full page with all new flat properties round-trip
    - Page with each sub-object round-trip
    - Backward compatibility: serialize a page with only the existing required fields, deserialize, assert new boolean defaults are true where expected

    In `ContentTreeBuilder.cs`, add a `BuildPageWithAllProperties()` helper that creates a page with every new property populated (use for round-trip tests).

    **Build and run tests:** `dotnet build && dotnet test tests/DynamicWeb.Serializer.Tests --no-build -v q`
  </action>
  <verify>
    <automated>dotnet test tests/DynamicWeb.Serializer.Tests --filter "DtoTests|YamlRoundTripTests" -v q</automated>
  </verify>
  <done>
    - 4 new sub-record files exist in Models/
    - SerializedPage has ~16 new flat properties + 4 sub-object references
    - Allowclick, Allowsearch, ShowInSitemap, ShowInLegend default to true
    - ActiveFrom, ActiveTo are nullable DateTime defaulting to null
    - All new and existing unit tests pass
    - YAML round-trip preserves all new properties including sub-objects
    - Old YAML without new properties deserializes with correct defaults
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend ContentMapper.MapPage() to populate all new properties</name>
  <files>
    src/DynamicWeb.Serializer/Serialization/ContentMapper.cs
  </files>
  <action>
    **Extend MapPage() return expression** (per PAGE-01, ECOM-01) -- add after existing `TreeSection = page.TreeSection` line:

    ```csharp
    NavigationTag = page.NavigationTag,
    ShortCut = page.ShortCut,
    Hidden = page.Hidden,
    Allowclick = page.Allowclick,
    Allowsearch = page.Allowsearch,
    ShowInSitemap = page.ShowInSitemap,
    ShowInLegend = page.ShowInLegend,
    SslMode = page.SslMode,
    ColorSchemeId = page.ColorSchemeId,
    ExactUrl = page.ExactUrl,
    ContentType = page.ContentType,
    TopImage = page.TopImage,
    DisplayMode = page.DisplayMode.ToString(),
    ActiveFrom = page.ActiveFrom,
    ActiveTo = page.ActiveTo,
    PermissionType = page.PermissionType,
    Seo = new SerializedSeoSettings
    {
        MetaTitle = page.MetaTitle,
        MetaCanonical = page.MetaCanonical,
        Description = page.Description,
        Keywords = page.Keywords,
        Noindex = page.Noindex,
        Nofollow = page.Nofollow,
        Robots404 = page.Robots404
    },
    UrlSettings = new SerializedUrlSettings
    {
        UrlDataProviderTypeName = page.UrlDataProviderTypeName,
        UrlDataProviderParameters = page.UrlDataProviderParameters,
        UrlIgnoreForChildren = page.UrlIgnoreForChildren,
        UrlUseAsWritten = page.UrlUseAsWritten
    },
    Visibility = new SerializedVisibilitySettings
    {
        HideForPhones = page.HideForPhones,
        HideForTablets = page.HideForTablets,
        HideForDesktops = page.HideForDesktops
    },
    NavigationSettings = MapNavigationSettings(page.NavigationSettings),
    ```

    **Add private helper method** at bottom of ContentMapper class (before the Helpers region):

    ```csharp
    /// <summary>
    /// Maps DW PageNavigationSettings to DTO. Returns null when UseEcomGroups is false
    /// (DW only populates NavigationSettings when ecommerce navigation is enabled).
    /// </summary>
    private static SerializedNavigationSettings? MapNavigationSettings(
        Dynamicweb.Content.PageNavigationSettings? navSettings)
    {
        if (navSettings == null || !navSettings.UseEcomGroups)
            return null;

        return new SerializedNavigationSettings
        {
            UseEcomGroups = true,
            ParentType = navSettings.ParentType.ToString(),
            Groups = navSettings.Groups,
            ShopID = navSettings.ShopID,
            MaxLevels = navSettings.MaxLevels,
            ProductPage = navSettings.ProductPage,
            NavigationProvider = navSettings.NavigationProvider,
            IncludeProducts = navSettings.IncludeProducts
        };
    }
    ```

    Add `using DynamicWeb.Serializer.Models;` if not already present (it should be).

    **Build:** `dotnet build` to confirm compilation. Full test suite should still pass.
  </action>
  <verify>
    <automated>dotnet build src/DynamicWeb.Serializer && dotnet test tests/DynamicWeb.Serializer.Tests --no-build -v q</automated>
  </verify>
  <done>
    - ContentMapper.MapPage() populates all ~30 new properties from DW Page
    - MapNavigationSettings() returns null when UseEcomGroups is false
    - MapNavigationSettings() returns populated DTO when UseEcomGroups is true
    - DisplayMode serialized as string ("Normal" or "List")
    - ParentType serialized as string ("Groups" or "Shop")
    - All tests pass (no regressions)
  </done>
</task>

</tasks>

<verification>
- `dotnet build` compiles without errors
- `dotnet test tests/DynamicWeb.Serializer.Tests -v q` -- all tests pass
- SerializedPage record has all ~30 new properties
- 4 sub-record files exist in Models/
- ContentMapper.MapPage() references all new properties
</verification>

<success_criteria>
- All ~30 missing page properties are represented in the DTO with correct types and defaults
- ContentMapper serializes all properties from DW Page objects
- NavigationSettings only serialized when UseEcomGroups=true
- Boolean defaults prevent backward compatibility breakage with old YAML
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/23-full-page-properties-navigation-settings/23-01-SUMMARY.md`
</output>
