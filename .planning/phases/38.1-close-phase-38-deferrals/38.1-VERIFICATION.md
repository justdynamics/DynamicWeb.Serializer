---
phase: 38.1-close-phase-38-deferrals
verified: 2026-04-21T18:00:00Z
status: gaps_found
score: 8/10 must-haves verified
overrides_applied: 0
gaps:
  - truth: "Live Swift 2.2 → CleanDB round-trip under strictMode: true returns HTTP 200 on all 4 API calls (serialize deploy, serialize seed, deserialize deploy, deserialize seed)"
    status: failed
    reason: "Deserialize Deploy returned HTTP 400 with 57 strict-mode escalations — 10 orphan PropertyItem GUIDs + 47 unresolvable page ID occurrences (20 distinct IDs). These are pre-existing content-link orphans categorized as out-of-scope by Phase 38-03 (lines 113-114) and newly surfaced by Phase 38-05's removal of acknowledgedOrphanPageIds. None of the 57 escalations originate from the 4 named Phase 38 deferrals."
    artifacts:
      - path: ".planning/phases/38.1-close-phase-38-deferrals/38.1-01-e2e-results.md"
        issue: "Deserialize Deploy: HTTP 400, 57 escalations — 10 PropertyItem GUIDs + 47 orphan link occurrences across 20 page IDs (1,2,4,16,19,21,23,33,34,37,40,41,42,44,48,60,97,98,104,113)"
    missing:
      - "B.5.2: Extend BaselineLinkSweeper to cover all link JSON shapes on serialize side for 20 orphan page IDs beyond the SelectedValue/Default.aspx patterns already covered"
      - "ITEM-01: Add pre-serialize sweep detecting orphan PropertyItem GUIDs (10 identified) — fail at source or clean via SQL script"
  - truth: "The smoke tool exits 0 AND exercises at least one page (not the vacuous 'Nothing to test' pass)"
    status: failed
    reason: "Smoke tool output: 'No active pages found under area 3. Nothing to test.' Exit code 0 vacuous. The Content deserialize aborted (HTTP 400) before writing Page rows, leaving Page=0 in CleanDB. This is downstream of the Deserialize Deploy gap — the smoke gate cannot be met until the 57 escalations are resolved."
    artifacts:
      - path: ".planning/phases/38.1-close-phase-38-deferrals/38.1-01-e2e-results.md"
        issue: "Smoke tool reports vacuous pass (Page=0 after Content abort). Same failure mode as Phase 38-05 final E2E."
    missing:
      - "Smoke gate is blocked until Deserialize Deploy returns HTTP 200 (i.e., until B.5.2 + ITEM-01 are resolved)"
---

# Phase 38.1: Close Phase 38 Deferrals — Verification Report

**Phase Goal:** Close the four Phase 38 deferrals (B.5.1 SelectedValue paragraph-ID, B.4.1 SHOP19 FK orphan, B.3.1 7-column schema drift, GRID-01 142 stale GridRows) + W6 ParagraphIdCollector extraction + ENVIRONMENT→NOT-SERIALIZED bucket rename, and verify closure with a live Swift 2.2 → CleanDB round-trip under strictMode: true.
**Verified:** 2026-04-21T18:00:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | HTTP 200 on all 4 API calls (serialize deploy/seed, deserialize deploy/seed) | FAILED | Deserialize Deploy: HTTP 400, 57 escalations (10 PropertyItem GUIDs + 47 orphan page-ID link occurrences). Serialize Deploy: HTTP 200. Serialize Seed: HTTP 200. Deserialize Seed: HTTP 200. |
| 2 | EcomProducts preservation: source 2051 == target 2051 | VERIFIED | e2e-results.md: EcomProducts 2051 → 2051 confirmed. Deserialize Seed returned HTTP 200 with 3489 rows created. |
| 3 | Zero escalated strict-mode warnings from the 4 named deferrals (B.5.1, B.4.1, B.3.1, GRID-01) | VERIFIED | e2e-results.md §"Deferral closure": all 4 deferrals explicitly CLOSED. Serialize Deploy HTTP 200 with 0 escalations — B.5.1/B.4.1/GRID-01 silent at serialize. Both Deserializes show zero B.3/B.3.1 schema-drift escalations (post-ALTER). |
| 4 | Smoke tool exits 0 AND exercises at least one page | FAILED | Smoke output: "No active pages found under area 3. Nothing to test." Vacuous pass. Page=0 because Content deserialization aborted at HTTP 400 before writing Page rows. |
| 5 | BaselineLinkSweeper SelectedValue loop validates paragraph IDs against validParagraphIds | VERIFIED | `BaselineLinkSweeper.cs` line 159: `if (validIds.Contains(id) || validParagraphIds.Contains(id))` — dual-check confirmed. Regression test `Sweep_SelectedValueJson_ParagraphId_Resolved` models exact `{"SelectedValue":"15717","LinkType":"paragraph"}` fixture. |
| 6 | SHOP19 orphan row deleted; re-serialized baseline contains no GROUP253$$SHOP19.yml | VERIFIED | Script 06 output: "SHOP19/GROUP253 rows before: 1 → after: 0", COMMIT. Serialize Deploy log: "Cleaned 1 stale file(s)" = removed GROUP253$$SHOP19.yml. CleanDB post-deserialize: 0 EcomShopGroupRelation orphans. |
| 7 | 142 GridRow rows deleted; post-delete count is 0 | VERIFIED | Script 07 output: "Stale-email GridRow rows before: 142 → after: 0", COMMIT. Baseline no longer contains stale-email GridRow YAMLs. |
| 8 | env-bucket.md enumerates 7 drift columns (GroupPageIDRel + 6 EcomProducts) | VERIFIED | `docs/baselines/env-bucket.md` lines 218-224: all 7 columns tabulated (GroupPageIDRel, ProductPeriodId, ProductVariantGroupCounter, ProductPriceMatrixPeriod, ProductOptimizedFor, MyVolume, MyDouble). |
| 9 | ENVIRONMENT bucket renamed to NOT-SERIALIZED across 3 files | VERIFIED | `docs/baselines/Swift2.2-baseline.md`: contains "NOT-SERIALIZED" and "formerly "ENVIRONMENT"" rationale blockquote. `docs/baselines/env-bucket.md`: all bucket references use NOT-SERIALIZED. `.planning/STATE.md`: three-bucket split documented as NOT-SERIALIZED. One intentional ALLCAPS ENVIRONMENT retained inside rationale blockquote per D-38.1-17. |
| 10 | ParagraphIdCollector shared helper exists; both call sites use it; tests green | VERIFIED | `Infrastructure/ParagraphIdCollector.cs` exists (32+ lines, substantive). `BaselineLinkSweeper.cs` line 51: `ParagraphIdCollector.Visit(allPages, ...)`. `InternalLinkResolver.cs` lines 51, 160: `ParagraphIdCollector.Visit(...)`. All existing tests reported green in SUMMARY. |

**Score: 8/10 truths verified**

---

### Gap Root-Cause Analysis

Truths 1 and 4 share a single root cause: **20 pre-existing orphan page IDs × 47 link occurrences + 10 orphan PropertyItem GUIDs** that were previously acknowledged via `acknowledgedOrphanPageIds: [15717]` (Phase 38-05 removed this ack) and categorized as "pre-existing, not Wave-3 scope" in Phase 38-03 e2e-results.md lines 113-114.

This gap is **explicitly outside the 4 named deferrals** this phase was chartered to close. The four named deferrals (B.5.1, B.4.1, B.3.1, GRID-01) are all verified CLOSED by live E2E evidence. The wider gap surfaced legitimately when Phase 38-05 removed the ack entry that had been masking it.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/DynamicWeb.Serializer/Infrastructure/ParagraphIdCollector.cs` | Shared tree-walker for paragraph ID collection | VERIFIED | Exists, 32 lines, `internal static class ParagraphIdCollector` with `Visit(IEnumerable<SerializedPage>, Action<SerializedParagraph>)`. |
| `src/DynamicWeb.Serializer/Infrastructure/BaselineLinkSweeper.cs` | Dual-check in SelectedValue loop; uses ParagraphIdCollector | VERIFIED | Contains `validParagraphIds.Contains(` at line 159; calls `ParagraphIdCollector.Visit` at line 51. |
| `src/DynamicWeb.Serializer/Serialization/InternalLinkResolver.cs` | Calls ParagraphIdCollector | VERIFIED | Contains `ParagraphIdCollector.` at lines 51 and 160. |
| `tests/DynamicWeb.Serializer.Tests/Infrastructure/BaselineLinkSweeperTests.cs` | Regression test with `"SelectedValue":"15717"` fixture | VERIFIED | Contains `{"SelectedValue":"15717","LinkType":"paragraph"}` fixture at line 225; test `Sweep_SelectedValueJson_ParagraphId_Resolved` at line 194. |
| `tools/swift22-cleanup/06-delete-orphan-ecomshopgrouprelation.sql` | SHOP19 FK orphan cleanup with BEGIN TRAN | VERIFIED | Exists, contains `BEGIN TRAN`, `BEGIN TRANSACTION`, `@before=1` assertion, DELETE predicate. |
| `tools/swift22-cleanup/07-delete-stale-email-gridrows.sql` | 142 stale-email GridRow cleanup | VERIFIED | Exists, contains `GridRowDefinitionId IN` predicate, `@before=142` assertion. |
| `tools/swift22-cleanup/README.md` | Rows 06 and 07 documented with ordering | VERIFIED | Contains `| 06 |` and `| 07 |` table rows; row 07 note: "Must run after 05." |
| `docs/baselines/env-bucket.md` | 7 drift columns + NOT-SERIALIZED rename | VERIFIED | Contains `NOT-SERIALIZED` throughout; 7 drift columns tabulated (lines 218-224). |
| `docs/baselines/Swift2.2-baseline.md` | NOT-SERIALIZED bucket rename | VERIFIED | Contains `NOT-SERIALIZED` and `formerly "ENVIRONMENT"` rationale blockquote. |
| `.planning/phases/38.1-close-phase-38-deferrals/38.1-01-e2e-results.md` | Live E2E observed-state capture | VERIFIED | Exists; contains `HTTP 200` (4+ occurrences) and `2051`; disposition `open-with-gap` with precise gap description. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `BaselineLinkSweeper.cs` | `ParagraphIdCollector.Visit` | Static call replacing inline walker | VERIFIED | Line 51: `ParagraphIdCollector.Visit(allPages, para => { ... })`. Note: PLAN frontmatter specifies pattern `ParagraphIdCollector\.CollectSourceParagraphIds` but implementation uses `.Visit` — this is the correct method name per the extracted class. Same intent, different finalized signature per D-38.1-14. |
| `InternalLinkResolver.cs` | `ParagraphIdCollector.Visit` | Static call | VERIFIED | Lines 51 and 160: `ParagraphIdCollector.Visit(...)`. |
| `tools/swift22-cleanup/README.md` | `05-null-stale-template-refs.sql` ordering | README documents 07 must run after 05 | VERIFIED | Row 07 in README: "Must run after 05." |

**Note on key_links pattern mismatch:** The PLAN frontmatter specifies pattern `ParagraphIdCollector\.CollectSourceParagraphIds` for the BaselineLinkSweeper link. The actual implementation uses `ParagraphIdCollector.Visit` — the signature evolved during implementation per D-38.1-14. Both call sites correctly use `.Visit`. This is a plan wording artifact, not a wiring gap.

---

### Data-Flow Trace (Level 4)

Not applicable. Phase deliverables are: cleanup SQL scripts (no dynamic data rendering), a static helper (ParagraphIdCollector), sweeper logic extension, and documentation. No components rendering dynamic data from a data source.

---

### Behavioral Spot-Checks

| Behavior | Evidence Source | Result | Status |
|----------|----------------|--------|--------|
| B.5.1: SelectedValue paragraph-ID resolves | `38.1-01-e2e-results.md` — Serialize Deploy HTTP 200, zero 15717-style escalations | Confirmed CLOSED | PASS |
| B.4.1: SHOP19 orphan deleted | Script 06 log: 1→0, COMMIT; CleanDB post-deserialize: 0 EcomShopGroupRelation orphans | Confirmed CLOSED | PASS |
| B.3.1: Schema drift resolved | ALTER TABLE applied; zero schema-drift escalations in both Deserialize calls | Confirmed CLOSED | PASS |
| GRID-01: 142 GridRows deleted | Script 07 log: 142→0, COMMIT | Confirmed CLOSED | PASS |
| W6: ParagraphIdCollector adopted | Both call sites use `.Visit`; SUMMARY reports all pre-existing tests green | PASS | PASS |
| NOT-SERIALIZED rename complete | grep confirms across all 3 files; rationale blockquote preserved | PASS | PASS |
| Deserialize Deploy: HTTP 200 | HTTP 400 with 57 pre-existing orphan escalations | FAILED | FAIL |
| Smoke tool: non-vacuous | "Nothing to test" vacuous (Page=0, downstream of HTTP 400) | FAILED | FAIL |

---

### Requirements Coverage

`phase_req_ids: null` — no REQUIREMENTS.md traceability declared for Phase 38.1 per PLAN frontmatter. All deliverables traced to ROADMAP.md §Phase 38.1 success criteria and CONTEXT.md decisions (D-38.1-01 through D-38.1-20). No orphaned requirements.

---

### Anti-Patterns Found

No stub anti-patterns detected in phase deliverables. All created files are substantive:
- `ParagraphIdCollector.cs` — complete recursive tree-walker with visitor pattern
- `06-*.sql` — complete transaction-wrapped DELETE with count assertions
- `07-*.sql` — complete transaction-wrapped DELETE with count assertions
- Documentation changes — full content replacements, not placeholders

---

### Gaps Summary

**Two truths fail, both from the same pre-existing root cause.**

The four deferrals this phase was chartered to close (B.5.1, B.4.1, B.3.1, GRID-01) are **all demonstrably closed by live E2E evidence**. The W6 ParagraphIdCollector extraction and ENVIRONMENT→NOT-SERIALIZED rename are also complete.

The round-trip fails at Deserialize Deploy (HTTP 400) due to **57 strict-mode escalations originating from pre-existing content-link orphans** that are outside this phase's scope:
- 47 "Unresolvable page ID N in link" occurrences across 20 distinct page IDs (1,2,4,16,19,21,23,33,34,37,40,41,42,44,48,60,97,98,104,113)
- 10 "Could not load PropertyItem for page GUID" errors

These were previously masked by `acknowledgedOrphanPageIds: [15717]` (removed in Phase 38-05) and explicitly categorized as "pre-existing, not Wave-3 scope" in Phase 38-03 e2e-results.md. Phase 38.1 closed the one acknowledged item (15717 via B.5.1), but the ack removal exposed 20 additional orphan IDs that no prior phase scoped.

The smoke gate failure is purely downstream: with Page=0 (Content aborted before page writes), the smoke tool has nothing to test.

**Follow-up required (not yet in ROADMAP.md):**
- **B.5.2** — Extend BaselineLinkSweeper to detect the 20 additional orphan page ID link shapes at serialize time
- **ITEM-01** — Add pre-serialize sweep or source-cleanup SQL for 10 orphan PropertyItem GUIDs

Both follow-ups mirror the B.5.1 approach (at-source fix, not target-side ack lists). The fix shape is fully known from the E2E evidence.

---

_Verified: 2026-04-21T18:00:00Z_
_Verifier: Claude (gsd-verifier)_
