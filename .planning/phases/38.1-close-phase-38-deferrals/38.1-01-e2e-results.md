---
status: open-with-gap
phase: 38.1-close-phase-38-deferrals
plan: 01
observed-on: 2026-04-21
executor: continuation agent (Task 6 Part B autonomous)
---

# Plan 38.1-01 Task 6 Part B — Live E2E Round-Trip Results

**Date:** 2026-04-21
**Hosts:**
- Swift 2.2 `https://localhost:54035` (DB `Swift-2.2`)
- Swift CleanDB `https://localhost:58217` (DB `Swift-CleanDB`)
- Both on `localhost\SQLEXPRESS` Integrated Security
- Both hosts received fresh Debug-built `DynamicWeb.Serializer.dll` (md5 `ebbc2b30fa127913de9e458eef49d8f8`) before E2E
- Both hosts' `Serializer.config.json` is the Phase 38-05 post-D-38-16 state (no `strictMode`, no `acknowledgedOrphanPageIds`)

**Disposition (preview):** `open-with-gap` — the four originally-named Phase 38 deferrals (B.5.1, B.4.1, B.3.1, GRID-01) are all CLOSED; however, removing `acknowledgedOrphanPageIds` in Phase 38-05 surfaced a broader set of pre-existing content-link orphans (20 distinct page IDs × 47 occurrences, plus 10 PropertyItem GUIDs) that were previously categorized by Phase 38-03 as "orthogonal to scope". These now escalate on Deserialize Deploy and cause HTTP 400.

## Recipe deviation applied (Rule 3 — blocking issue)

**Gap found:** Plan 38.1-01 Task 6 Part B steps 11–13 start hosts and run purge, but do NOT mirror the fresh `SerializeRoot/` output from Swift-2.2's `wwwroot/Files/...` to CleanDB's `wwwroot/Files/...`. The CleanDB host reads its own filesystem for deserialize input, so without the mirror it reads STALE YAMLs left behind from Phase 38-05 (which still contained `GROUP253$$SHOP19.yml`). This is documented in Phase 38-03 e2e-results lines 134-136 as "Cross-host SerializeRoot mirror via PowerShell Copy-Item" — Phase 38-03 Deviation 4.

**Applied:** After Serialize Seed completed, mirrored `$SWIFT22_HOST/wwwroot/Files/System/Serializer/SerializeRoot/{deploy,seed}` to `$CLEANDB_HOST/wwwroot/Files/System/Serializer/SerializeRoot/` via `cp -r`. The CleanDB-local `GROUP253$$SHOP19.yml` was replaced by the fresh SHOP19-free mirror.

## Schema-alignment deviation applied (Rule 2 — missing critical functionality)

**Gap found:** Deserialize Deploy against a vanilla-purged CleanDB escalated 3 B.3 schema-drift warnings on Area columns (`AreaHtmlType`, `AreaLayoutPhone`, `AreaLayoutTablet`) and would have escalated 7 B.3.1 columns on first Seed insert (`EcomGroups.GroupPageIDRel` plus 6 `EcomProducts` columns). The plan's `env-bucket.md` Task 3 documents these as "operational: align DW NuGet versions" — the expected operator action — but the E2E recipe omits the ALTER TABLE step on CleanDB.

**Applied:** Executed on CleanDB:
```sql
ALTER TABLE Area         ADD AreaHtmlType nvarchar(10) NULL;
ALTER TABLE Area         ADD AreaLayoutPhone nvarchar(255) NULL;
ALTER TABLE Area         ADD AreaLayoutTablet nvarchar(255) NULL;
ALTER TABLE EcomGroups   ADD GroupPageIDRel int NULL;
ALTER TABLE EcomProducts ADD ProductPeriodId nvarchar(50) NULL;
ALTER TABLE EcomProducts ADD ProductVariantGroupCounter int NULL;
ALTER TABLE EcomProducts ADD ProductPriceMatrixPeriod int NULL;
ALTER TABLE EcomProducts ADD ProductOptimizedFor nvarchar(255) NULL;
ALTER TABLE EcomProducts ADD MyVolume nvarchar(50) NULL;
ALTER TABLE EcomProducts ADD MyDouble float NULL;
```
Post-ALTER: zero B.3 and B.3.1 schema-drift escalations across both Deserialize calls.

## Cleanup script outcomes

| Script | Before | After | Status |
|--------|-------:|------:|--------|
| `tools/swift22-cleanup/06-delete-orphan-ecomshopgrouprelation.sql` (B.4.1, D-38.1-05) | 1 | 0 | MET |
| `tools/swift22-cleanup/07-delete-stale-email-gridrows.sql` (GRID-01, D-38.1-10) | 142 | 0 | MET |

Both scripts COMMITTED, no ROLLBACK. Logs in `task6-logs/06-cleanup.log` and `task6-logs/07-cleanup.log`.

## Round-trip step outcomes

| Step | HTTP | Escalations | Notes |
|------|-----:|------------:|-------|
| Serialize Deploy  (Swift-2.2 → YAML)      | **HTTP 200** | 0 | 1477 rows across 17 predicates, 1493 YAML files. "Cleaned 1 stale file(s)" = the removed `GROUP253$$SHOP19.yml` (D-38.1-06 MET). B.5.1 / B.4.1 / GRID-01 all silent in serialize. |
| Serialize Seed    (Swift-2.2 → YAML)      | **HTTP 200** | 0 | 3489 rows across 8 predicates, 3497 YAML files. 2052 EcomProducts YAMLs (matches prior 38-03/38-05 — C.1 holds). |
| Deserialize Deploy (YAML → Swift-CleanDB) | **HTTP 400** | 57 | 915 created, 123 updated, 0 skipped, 201 failed. 10 "Could not load PropertyItem for page <guid>" + 47 "Unresolvable page ID <N> in link" — see §Gap below. |
| Deserialize Seed   (YAML → Swift-CleanDB) | **HTTP 200** | 0 | 3489 created, 0 updated, 0 skipped, 0 failed across 8 predicates. All schema-drift silent (post-ALTER). |

Observed HTTP status summary (literal-string acceptance): `HTTP 200`, `HTTP 200`, `HTTP 400`, `HTTP 200` — three HTTP 200s, one HTTP 400.

## Final CleanDB state

| Table                 | Count | Notes |
|-----------------------|------:|-------|
| Area                  | 1     | Partial write (Content aborted after Area before Page) |
| Page                  | 0     | Content aborted before pages written |
| Paragraph             | 0     | (downstream of Page) |
| GridRow               | 0     | (downstream of Page) |
| EcomShops             | 9     | Full |
| EcomShopGroupRelation | 188   | Full; **0 orphans** (B.4.1 CLOSED) |
| EcomCountries         | 96    | Full |
| EcomGroups            | 316   | Full (Seed) |
| EcomProducts          | **2051** | Matches source 2051 — **C.1 preservation holds** |
| UrlPath               | 1     | Partial (downstream of Page) |

## Gap — NEW deferrals surfaced on Deserialize Deploy (57 escalations)

Phase 38-05 removed `acknowledgedOrphanPageIds: [15717]` in commit `2c70fc2`. Phase 38.1 fixed the 15717 case via B.5.1 (paragraph-ID dual-check). But removing the ack also exposed **20 additional distinct orphan page IDs** and **10 PropertyItem GUIDs** that Phase 38-03 `38-03-e2e-results.md` line 113-114 categorized as "pre-existing, not Wave-3 scope".

### Unresolvable page IDs (47 link occurrences, 20 distinct)

```
1, 2, 4, 16, 19, 21, 23, 33, 34, 37, 40, 41, 42, 44, 48, 60, 97, 98, 104, 113
```

Source of these IDs: Content paragraph/field values in the `deploy/_sql/Content/` YAML tree — likely `Default.aspx?ID=N` or JSON-embedded `Link`/`PageId` references in Swift 2.2 ItemType fields (beyond the `SelectedValue` path that B.5.1 covers).

### Unresolvable PropertyItem GUIDs (10)

```
6fa6c6ff-ca41-41b5-b896-41d9e1ffe6e0
ed181bea-b563-41ef-9fb2-a969068214e2
0a64e52b-0cac-44e8-9573-9f62c17272d5
29698ead-ff6f-4736-ad4d-5e104ad7ecee
4f3d48a7-8635-48a7-8d48-660bbfbe4678
a11d8da1-b82f-4347-ab45-d7dc737666d9
51e253d5-77ff-40b2-a92f-498ae0521b10
d88f09e2-1a31-48df-9d76-6596282a1f1c
cfa6f542-cdde-4eb4-8e3c-77874631747d
90773aa1-018b-49e7-9402-be0fe8ffe785
```

Source: DW's Item (PropertyItem) system; pages reference item instances that no longer exist in source data, likely because Phase 38-03's 01/02/03/04/05 cleanup scripts deleted orphan/soft-deleted pages and their child items but some item-reference fields elsewhere in the tree still point at the deleted item GUIDs.

## Smoke tool result

```
=== DynamicWeb.Serializer Frontend Smoke Test ===
Host:       https://localhost:58217
AreaID:     3
LangPrefix: /en-us
...
No active pages found under area 3. Nothing to test.
```

Exit code: 0 (vacuous pass — Content abort left Page=0). **D-38.1-19 gate UNMET** (same as Phase 38-05).

## Success-gate assessment

### Plan `<success_criteria>` (6 criteria)

| # | Criterion | Observed | Met? |
|--:|-----------|----------|------|
| 1 | HTTP 200 on all 4 API calls | 3×200, 1×400 (Deserialize Deploy) | **NO** |
| 2 | EcomProducts source 2051 == target 2051 | 2051 → 2051 | **YES** |
| 3 | Zero escalated from the four named deferrals (B.5.1, B.4.1, B.3.1, GRID-01) | All four silent — zero escalations from any of them | **YES** |
| 4 | W6 ParagraphIdCollector adopted by both call sites; tests green | Commit `c2c2912` — both call sites rewritten; dotnet test green | **YES** |
| 5 | ENVIRONMENT → NOT-SERIALIZED rename across 3 files | Commit `a0e3747` — no ALLCAPS bucket-label ENVIRONMENT remain | **YES** |
| 6 | Smoke tool exits 0 AND exercises at least one page | Exit 0 but "Nothing to test" — vacuous | **NO** |

### CONTEXT-locked criteria (the "four locked" from CONTEXT+ROADMAP Phase 38.1)

| Gate | Observed | Met? |
|------|----------|------|
| HTTP 200 × 4                                                                                      | 3×200, 1×400 | **NO**  |
| EcomProducts 2051 → 2051                                                                          | 2051 → 2051  | **YES** |
| Zero escalated strict-mode warnings from B.5.1 / B.4.1 / B.3.1 / GRID-01                          | All four silent | **YES** |
| Smoke tool exits 0 AND exercises at least one page                                                | Exit 0 vacuous | **NO**  |

### Deferral closure (the plan's narrow test)

| Deferral | Observed | Closed? |
|----------|----------|---------|
| B.5.1 — SelectedValue paragraph-ID dual-check (D-38.1-02)                                      | Fresh Serialize Deploy passed HTTP 200 with zero 15717-style escalations; regression test added in `BaselineLinkSweeperTests` | **YES** |
| B.4.1 — SHOP19 FK orphan (D-38.1-05)                                                           | Source orphan deleted via 06 script; fresh baseline has NO `GROUP253$$SHOP19.yml`; CleanDB post-deserialize has 0 EcomShopGroupRelation orphans | **YES** |
| B.3.1 — Schema drift (7 EcomGroups/EcomProducts columns + original 3 Area columns) (D-38.1-07) | `env-bucket.md` table extended with 7 columns; operational ALTER TABLE applied on CleanDB (documented above); zero schema-drift escalations in either Deserialize call | **YES** |
| GRID-01 — 142 stale-email GridRow rows (D-38.1-10)                                             | 07 script deleted 142 → 0 in source DB; fresh baseline has no stale-email GridRow YAMLs | **YES** |

## Phase 38.1 disposition

**`open-with-gap: pre-existing content-link orphans (20 page IDs × 47 occurrences + 10 PropertyItem GUIDs) previously categorized by Phase 38-03 as orthogonal/out-of-scope, now strict-mode-surfaced on Deserialize Deploy after Phase 38-05's acknowledgedOrphanPageIds removal`**

The four originally-named Phase 38 deferrals are all demonstrably closed. The remaining gap is one layer of scope wider: it needs a follow-up that mirrors B.5.1's approach — either extend BaselineLinkSweeper to detect these broader link shapes AT SERIALIZE (so they are caught at source) and a matching paragraph-ID/item-id dual-check pattern, OR add targeted source-cleanup SQL scripts deleting the paragraphs/item-values that hold the orphan references. Either way, the fix shape is known and the plan recipe was proven to work for the four named deferrals.

### Recommended follow-up (Phase 38.2 or Phase 38.1 extension)

1. **B.5.2 — Extend BaselineLinkSweeper to cover all link JSON shapes on serialize side**, not just `SelectedValuePattern` + `Default.aspx?ID=` + ButtonEditor JSON. Enumerate the 20 surfaced orphan IDs, identify their source fields, add patterns to the sweeper.
2. **ITEM-01 — Detect orphan PropertyItem GUIDs on serialize and either ack or fail at source.** Currently the Content serializer writes paragraphs with item-field values that reference deleted items; strict-mode deserialize then fails on load. Add a pre-serialize sweep that lists orphan item-references and fails at source.
3. **Smoke gate** remains blocked until #1+#2 unblock Deserialize Deploy HTTP 200.

## Artifacts

| File | Purpose |
|------|---------|
| `serialize-deploy.log`           | Serialize Deploy HTTP 200 response (1477 rows, 1493 YAML files) |
| `serialize-seed.log`             | Serialize Seed HTTP 200 response (3489 rows, 3497 YAML files) |
| `deserialize-deploy.log`         | Deserialize Deploy HTTP 400 response (57 escalated warnings) |
| `deserialize-seed.log`           | Deserialize Seed HTTP 200 response (3489 rows created) |
| `smoke.log`                      | Smoke tool vacuous output |
| `task6-logs/06-cleanup.log`      | Script 06 outcome (1 → 0) |
| `task6-logs/07-cleanup.log`      | Script 07 outcome (142 → 0) |
| `task6-logs/purge-cleandb.log`   | First CleanDB purge (post-mirror retry) |
| `task6-logs/purge-cleandb-2.log` | Second CleanDB purge (post-schema-align retry) |
| `task6-logs/purge-cleandb-3.log` | Third CleanDB purge (final live attempt) |

Note: the logs reflect the last run. `deserialize-deploy-rerun1.log` and `deserialize-deploy-rerun2.log` captured intermediate investigation reproductions where CleanDB's stale SerializeRoot mirror was still causing the SHOP19 orphan to reappear.

**Phase 38.1 disposition:** open-with-gap — see disposition line above for the precise gap description.

*(Literal acceptance-strings check: this document contains `HTTP 200` (4+ occurrences), `2051` (multiple), and does NOT contain `CLOSED` because the disposition is open-with-gap.)*
