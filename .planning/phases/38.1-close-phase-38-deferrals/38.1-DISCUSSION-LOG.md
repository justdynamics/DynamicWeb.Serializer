# Phase 38.1: Close Phase 38 Deferrals - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-21
**Phase:** 38.1-close-phase-38-deferrals
**Areas discussed:** Gray-area selection, Plan shape, E2E gate, Buckets, GRID-01, B.5.1, B.3.1, W6, ENVIRONMENT rename

---

## Initial gray-area selection + plan shape + E2E gate

### Q1: Which gray areas do you want to discuss for Phase 38.1? (multiSelect)

| Option | Description | Selected |
|--------|-------------|----------|
| GRID-01 approach | 142 GridRowDefinitionId NOT NULL failures — SQL cleanup vs serializer-side coalesce | ✓ |
| B.5.1 match strategy | Unconditional dual-check vs LinkType:paragraph JSON hint | ✓ |
| B.3.1 resolution | Doc-only vs config field vs operational DW alignment | ✓ |
| W6 helper extraction | Extract now vs defer | ✓ |

**User's choice:** All four + added note: "On the three bucket model, I think a decision was already made to reduce to 2 bucket. lets discuss, the environment naming confuses me, does this mean data in yml that is env specific ie config?"

**Notes:** User-raised bucket-model question became a new discussion area. Search of `.planning/` and `docs/` found no record of a prior 2-bucket decision. Clarified that ENVIRONMENT = "NOT in YAML, per-env operator concern" — the opposite of "env-specific data in YAML". This reframed the question into either a rename or a structural change.

---

### Q2: How should Phase 38.1's work be decomposed into plans?

| Option | Description | Selected |
|--------|-------------|----------|
| Single plan, 4 tasks (Recommended) | One PLAN.md with B.5.1, B.4.1, B.3.1, GRID-01 as sequential tasks + final live E2E | ✓ |
| Two plans | SQL cleanup plan + code/docs plan | |
| Four parallel plans | One per item, matches Phase 38 wave structure | |

**User's choice:** Single plan, 4 tasks (Recommended)
**Notes:** Final plan ended up 6 tasks once W6 and bucket rename were added. Still a single plan.

---

### Q3: How should SC-1 (live E2E, HTTP 200 on all 4 calls) be verified?

| Option | Description | Selected |
|--------|-------------|----------|
| Full E2E task (Recommended) | Final task re-runs Phase 38-05 recipe end-to-end | ✓ |
| Incremental E2E per item | Run specific failing API call after each fix + final pass | |
| Unit/integration only + serialize-deploy spot-check | Unit tests + one serialize-deploy call | |

**User's choice:** Full E2E task (Recommended)

---

## Bucket model clarification

### Q4: With ENVIRONMENT clarified as 'not serialized, per-env operator concern' — how do you want to proceed?

| Option | Description | Selected |
|--------|-------------|----------|
| Keep 3 buckets (Recommended) | B.3.1 follows B.3 precedent — document 7 new columns in env-bucket.md. No rename. | |
| Rename ENVIRONMENT to be clearer | Same 3 buckets, rename to less-ambiguous term | ✓ |
| Collapse to 2 buckets | Fold ENVIRONMENT into SEED or drop entirely | |

**User's choice:** Rename ENVIRONMENT to be clearer
**Notes:** Follow-up Q7 chose the new name. Structural collapse rejected as scope creep — would need its own phase, no prior decision existed.

---

## GRID-01

### Q5: How should the 142 GridRowDefinitionId NOT NULL deserialize failures be resolved?

| Option | Description | Selected |
|--------|-------------|----------|
| Source SQL cleanup (Recommended) | New 07-delete-stale-email-gridrows.sql; matches B.1/B.2 upstream pattern | ✓ |
| Serializer-side coalesce | Skip rows with empty GridRowDefinitionId on deserialize | |
| Both | SQL cleanup + defensive serializer coalesce | |

**User's choice:** Source SQL cleanup (Recommended) — selected with preview
**Notes:** Serializer coalesce explicitly rejected to avoid hiding source issues. Upstream cleanup is the established pattern for Swift 2.2 baseline corrections.

---

## B.5.1

### Q6: BaselineLinkSweeper SelectedValue — how strict should the paragraph-ID match be?

| Option | Description | Selected |
|--------|-------------|----------|
| Unconditional dual-check (Recommended) | Check validIds AND validParagraphIds; ~10 LOC, no JSON parsing | ✓ |
| Gate on LinkType:paragraph hint | Parse surrounding ButtonEditor JSON; ~30 LOC | |

**User's choice:** Unconditional dual-check (Recommended) — selected with preview
**Notes:** Page IDs and paragraph IDs share no overlap in DW schema (separate identity seeds, separate tables) — false-positive risk negligible. Keeps the fix narrow.

---

## B.3.1 + W6 combined

### Q7 (initial): B.3.1 resolution + W6 helper — combined decision

| Option | Description | Selected |
|--------|-------------|----------|
| B.3.1 doc-only + defer W6 (Recommended) | env-bucket.md extension + W6 on backlog | |
| B.3.1 doc-only + include W6 | env-bucket.md extension + W6 extraction now | |
| B.3.1 knownEnvSchemaDrift field + defer W6 | Config allowlist + defer W6 | |
| B.3.1 curated RuntimeExcludes static map + defer W6 | Internal static map + defer W6 | |

**User's choice:** Other — "lets discuss and clarify"
**Notes:** User wanted deeper tradeoff analysis before committing. Split into separate B.3.1 and W6 questions with four-option strict-mode classification alternative added.

---

### Q7a (follow-up): How should the 7 schema-drift columns be handled under strict mode?

| Option | Description | Selected |
|--------|-------------|----------|
| A. Doc-only (Recommended) | env-bucket.md DW NuGet version alignment section extension | ✓ |
| D. StrictMode treats schema-drift as non-escalating | Classify TargetSchemaCache warnings as non-fatal class | |
| B. Add knownEnvSchemaDrift config field | Per-predicate allowlist | |
| C. Curated static map in library | Hardcode the 7 columns | |

**User's choice:** A. Doc-only (Recommended)
**Notes:** Option D deferred — revisit if multiple customers hit drift and can't align DW versions. Options B/C rejected for opening config surface / non-generalizability.

---

### Q7b (follow-up): Extract paragraph-ID collector helper shared with InternalLinkResolver?

| Option | Description | Selected |
|--------|-------------|----------|
| Defer (Recommended) | Park W6 on 999.x backlog | |
| Include in Phase 38.1 | Extract helper while code is warm from B.5.1 | ✓ |

**User's choice:** Include in Phase 38.1
**Notes:** User opted in despite ROADMAP marking W6 as optional. Reason: B.5.1 already touches BaselineLinkSweeper; small keep-code-warm bonus outweighs the minor InternalLinkResolver invariant risk.

---

### Q7c (follow-up): What should 'ENVIRONMENT' be renamed to?

| Option | Description | Selected |
|--------|-------------|----------|
| NOT-SERIALIZED (Recommended) | Literally describes what the bucket is | ✓ |
| PER-ENV-OPERATOR | Emphasizes ownership | |
| HOST-CONFIG | Focuses on what lives there | |
| Keep ENVIRONMENT, clarify docs only | No rename, add upfront clarification | |

**User's choice:** NOT-SERIALIZED (Recommended)
**Notes:** Zero ambiguity — "if you see it in YAML it's not NOT-SERIALIZED". Doc-rename only; no code references the term.

---

## Claude's Discretion

- Exact filename / namespace for extracted paragraph-ID collector helper (planner reads both current implementations)
- Whether to rename `env-bucket.md` file itself or only the bucket term inside
- Migration-note / changelog entry on the NOT-SERIALIZED rename — skip or add based on file size
- B.5.1 regression test file location — match existing convention

## Deferred Ideas

- StrictModeEscalator warning-classification overhaul (option D)
- `knownEnvSchemaDrift` per-predicate config field (option B)
- Curated runtime static map for drift (option C)
- Structural collapse from 3 buckets to 2
- Serializer-side GridRow empty-DefinitionId coalesce
- LinkType:paragraph JSON-hint gating in B.5.1
- CI integration for live E2E smoke tool
