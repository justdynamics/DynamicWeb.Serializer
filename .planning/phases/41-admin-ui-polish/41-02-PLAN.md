---
phase: 41-admin-ui-polish
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/DynamicWeb.Serializer/AdminUI/Tree/SerializerSettingsNodeProvider.cs
  - src/DynamicWeb.Serializer/AdminUI/Screens/ItemTypeListScreen.cs
  - src/DynamicWeb.Serializer/AdminUI/Screens/ItemTypeEditScreen.cs
  - src/DynamicWeb.Serializer/AdminUI/Screens/PredicateEditScreen.cs
  - docs/baselines/Swift2.2-baseline.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-11
tags:
  - admin-ui
  - rename
  - docs
must_haves:
  truths:
    - "Tree node display name reads 'Item Type Excludes' (not 'Item Types')"
    - "ItemTypeListScreen.GetScreenName() returns 'Item Type Excludes'"
    - "ItemTypeEditScreen.GetScreenName() returns 'Item Type Excludes - {SystemName}' when SystemName set, else 'Item Type Excludes'"
    - "Internal const ItemTypesNodeId remains 'Serializer_ItemTypes' (D-02 — DO NOT rename)"
    - "PredicateEditScreen Mode option labels are 'Deploy' and 'Seed' (no parenthetical suffixes)"
    - "docs/baselines/Swift2.2-baseline.md documents the intentional emptiness of excludeFieldsByItemType in the post-Phase-40 baseline"
  artifacts:
    - path: "src/DynamicWeb.Serializer/AdminUI/Tree/SerializerSettingsNodeProvider.cs"
      provides: "Tree node rename anchor (line 75)"
      contains: 'Name = "Item Type Excludes"'
    - path: "src/DynamicWeb.Serializer/AdminUI/Screens/ItemTypeListScreen.cs"
      provides: "List screen title rename (line 13)"
      contains: 'GetScreenName() => "Item Type Excludes"'
    - path: "src/DynamicWeb.Serializer/AdminUI/Screens/ItemTypeEditScreen.cs"
      provides: "Edit screen title rename (line 130-131); CreateFieldSelector NOT MODIFIED here — Plan 41-03 owns that"
      contains: 'Item Type Excludes - {Model.SystemName}'
    - path: "src/DynamicWeb.Serializer/AdminUI/Screens/PredicateEditScreen.cs"
      provides: "Mode option label cleanup (lines 88-89)"
      contains: 'Label = "Deploy"'
    - path: "docs/baselines/Swift2.2-baseline.md"
      provides: "Section explaining intentional emptiness of excludeFieldsByItemType + expanded excludeXmlElementsByType context"
      contains: "excludeFieldsByItemType"
  key_links:
    - from: "tree node (Name property)"
      to: "DW framework breadcrumb"
      via: "NavigationNode.Name flows automatically into breadcrumb segment"
      pattern: 'Name = "Item Type Excludes"'
    - from: "PredicateEditScreen Mode Select"
      to: "ListOption.Label"
      via: "framework renders option Label in dropdown UI"
      pattern: 'Label = "(Deploy|Seed)"'
---

<objective>
Apply five mechanical, low-risk surface-text fixes that close D-01 (rename), D-03 (baseline doc), and D-11 (Mode option-label cleanup):

1. Tree node display label rename
2. List screen title rename
3. Edit screen title rename (GetScreenName ONLY — do NOT touch CreateFieldSelector; that's Plan 41-03's territory)
4. Mode dropdown option labels: drop parenthetical suffixes
5. Baseline doc section documenting the intentional empty `excludeFieldsByItemType`

No production logic changes. No model type changes. No selector logic changes. After this plan ships, Plan 41-03 layers the substantive D-05/D-06/D-08/D-12/D-13 changes on top.

Purpose: deliver the user-visible polish surface in a small, easy-to-review change before the riskier merge-logic rework. Turns the D-01 + D-11 RED tests from Plan 41-01 GREEN, leaves D-05/D-06/D-12/D-13 still RED for Plan 41-03.

Output: 5 files modified, 4 source-text changes + 1 docs section. ~30% context.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-admin-ui-polish/41-CONTEXT.md
@.planning/phases/41-admin-ui-polish/41-RESEARCH.md
@.planning/phases/41-admin-ui-polish/41-PATTERNS.md
@.planning/phases/41-admin-ui-polish/41-01-SUMMARY.md
@src/DynamicWeb.Serializer/AdminUI/Tree/SerializerSettingsNodeProvider.cs
@src/DynamicWeb.Serializer/AdminUI/Screens/ItemTypeListScreen.cs
@src/DynamicWeb.Serializer/AdminUI/Screens/ItemTypeEditScreen.cs
@src/DynamicWeb.Serializer/AdminUI/Screens/PredicateEditScreen.cs
@docs/baselines/Swift2.2-baseline.md

<interfaces>
Anchor 1 — SerializerSettingsNodeProvider.cs lines 72-81 (current):
The const at line 27 (`public const string ItemTypesNodeId = "Serializer_ItemTypes";`) STAYS unchanged per D-02.
The string at line 75 (`Name = "Item Types",`) is the only rename target.

Anchor 2 — ItemTypeListScreen.cs line 13 (current):
`protected override string GetScreenName() => "Item Types";` — single-line rename.

Anchor 3 — ItemTypeEditScreen.cs lines 130-131 (current):
GetScreenName ternary — rename "Item Type:" → "Item Type Excludes -" and "Item Type" → "Item Type Excludes".
DO NOT touch lines 82-128 (CreateFieldSelector — Plan 41-03 owns that).

Anchor 4 — PredicateEditScreen.cs lines 83-91 (current):
Two ListOption.Label literals "Deploy (source-wins)" and "Seed (field-level merge)" — drop the parenthetical suffix.
DO NOT change Value strings (Value = nameof(DeploymentMode.Deploy/Seed) stays — Plan 41-03 owns the Mode property type change).

Anchor 5 — docs/baselines/Swift2.2-baseline.md (existing structure):
Phase 40 update at lines 64-68 already exists. New D-03 section slots in alongside.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename tree node + list/edit screen titles (D-01)</name>
  <files>src/DynamicWeb.Serializer/AdminUI/Tree/SerializerSettingsNodeProvider.cs, src/DynamicWeb.Serializer/AdminUI/Screens/ItemTypeListScreen.cs, src/DynamicWeb.Serializer/AdminUI/Screens/ItemTypeEditScreen.cs</files>
  <read_first>
    - src/DynamicWeb.Serializer/AdminUI/Tree/SerializerSettingsNodeProvider.cs (full file — confirm const block at lines 23-29 and locate the Name = "Item Types" string at line 75)
    - src/DynamicWeb.Serializer/AdminUI/Screens/ItemTypeListScreen.cs (small file; confirm GetScreenName at line 13)
    - src/DynamicWeb.Serializer/AdminUI/Screens/ItemTypeEditScreen.cs (full file — confirm GetScreenName at line 130-131; DO NOT touch CreateFieldSelector at lines 82-128 — Plan 41-03 owns that)
    - .planning/phases/41-admin-ui-polish/41-CONTEXT.md (D-01 + D-02 lock — internal const stays)
    - tests/DynamicWeb.Serializer.Tests/AdminUI/SerializerSettingsNodeProviderModeTreeTests.cs (the new fact added in 41-01 will turn GREEN with this change)
    - tests/DynamicWeb.Serializer.Tests/AdminUI/ItemTypeEditScreenTests.cs (the GetScreenName facts added in 41-01 will turn GREEN)
  </read_first>
  <action>
**Step 1 — SerializerSettingsNodeProvider.cs (line 75):**

Replace the literal:
`                Name = "Item Types",`

with:
`                Name = "Item Type Excludes",  // Phase 41 D-01: page manages per-ItemType field exclusions, not item types themselves. Const ItemTypesNodeId stays "Serializer_ItemTypes" per D-02.`

**DO NOT touch:**
- The const at line 27 (`public const string ItemTypesNodeId = "Serializer_ItemTypes";`) — D-02 explicitly forbids the rename to avoid churning `SerializerSettingsNodeProviderModeTreeTests` that asserts the const id.
- Any other `Name = "..."` strings in the file (Predicates, Embedded XML, Log Viewer are unaffected).

**Step 2 — ItemTypeListScreen.cs (line 13):**

Replace the literal line:
`    protected override string GetScreenName() => "Item Types";`

with:
`    protected override string GetScreenName() => "Item Type Excludes";  // Phase 41 D-01`

**Step 3 — ItemTypeEditScreen.cs (lines 130-131):**

Replace these two lines:
```
    protected override string GetScreenName() =>
        !string.IsNullOrWhiteSpace(Model?.SystemName) ? $"Item Type: {Model.SystemName}" : "Item Type";
```

with:
```
    protected override string GetScreenName() =>
        // Phase 41 D-01: page manages field exclusions for the item type, not the item type itself.
        !string.IsNullOrWhiteSpace(Model?.SystemName) ? $"Item Type Excludes - {Model.SystemName}" : "Item Type Excludes";
```

**Critical NON-changes:**
- ItemTypeEditScreen.cs CreateFieldSelector method (lines 82-128) is NOT touched in this plan. Plan 41-03 ships the merge fix.
- No const renames anywhere.
- No changes to NavigationNodePathProvider files (the path provider derives breadcrumb from `NavigationNode.Name` so the rename flows automatically — no separate edit needed).
  </action>
  <verify>
    <automated>cd C:\VibeCode\DynamicWeb.Serializer && dotnet build DynamicWeb.Serializer.sln</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Name = "Item Type Excludes"' src/DynamicWeb.Serializer/AdminUI/Tree/SerializerSettingsNodeProvider.cs` finds exactly 1 match at or near line 75
    - `grep -n 'public const string ItemTypesNodeId = "Serializer_ItemTypes"' src/DynamicWeb.Serializer/AdminUI/Tree/SerializerSettingsNodeProvider.cs` finds exactly 1 match at or near line 27 (const PRESERVED per D-02)
    - `grep -n 'GetScreenName() => "Item Type Excludes"' src/DynamicWeb.Serializer/AdminUI/Screens/ItemTypeListScreen.cs` finds exactly 1 match at or near line 13
    - `grep -n 'Item Type Excludes - {Model.SystemName}' src/DynamicWeb.Serializer/AdminUI/Screens/ItemTypeEditScreen.cs` finds exactly 1 match
    - `grep -c '"Item Type:"' src/DynamicWeb.Serializer/AdminUI/Screens/ItemTypeEditScreen.cs` returns 0 (old text fully replaced)
    - `grep -c '"Item Types"' src/DynamicWeb.Serializer/AdminUI/Screens/ItemTypeListScreen.cs` returns 0
    - `grep -c '"Item Types"' src/DynamicWeb.Serializer/AdminUI/Tree/SerializerSettingsNodeProvider.cs` returns 0
    - `dotnet build DynamicWeb.Serializer.sln` exits 0
    - `dotnet test --filter "FullyQualifiedName~SerializerSettingsNodeProviderModeTreeTests.GetSubNodes_UnderSerializeNode_ItemTypesNode_DisplayName_IsItemTypeExcludes" --no-build` exits 0 (test added in 41-01 turns GREEN)
    - `dotnet test --filter "FullyQualifiedName~ItemTypeEditScreenTests.GetScreenName" --no-build` exits 0 (both GetScreenName facts from 41-01 turn GREEN)
  </acceptance_criteria>
  <done>3 files modified. Build green. Three RED tests from 41-01 (tree-node rename, GetScreenName with-systemName, GetScreenName no-model) turn GREEN. Const `ItemTypesNodeId = "Serializer_ItemTypes"` is verifiably preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Drop parenthetical suffixes on Mode option labels (D-11)</name>
  <files>src/DynamicWeb.Serializer/AdminUI/Screens/PredicateEditScreen.cs</files>
  <read_first>
    - src/DynamicWeb.Serializer/AdminUI/Screens/PredicateEditScreen.cs (full file — confirm Mode Select at lines 83-91; the GetEditor switch is at line 79)
    - src/DynamicWeb.Serializer/AdminUI/Screens/SerializerSettingsEditScreen.cs (sibling — confirm CreateLogLevelSelect string-Value precedent if needed for context)
    - .planning/phases/41-admin-ui-polish/41-CONTEXT.md (D-11 lock: drop "(source-wins)" and "(field-level merge)" suffixes)
    - .planning/phases/41-admin-ui-polish/41-RESEARCH.md lines 305-321 (the canonical fix shape)
    - tests/DynamicWeb.Serializer.Tests/AdminUI/PredicateCommandTests.cs (the PredicateEditScreen_ModeOptions_HaveCleanLabels_PostPhase41 fact added in 41-01 will turn GREEN with this change)
  </read_first>
  <action>
In `src/DynamicWeb.Serializer/AdminUI/Screens/PredicateEditScreen.cs`, replace lines 83-91:

```
        nameof(PredicateEditModel.Mode) => new Select
        {
            SortOrder = OrderBy.Default,
            Options = new List<ListOption>
            {
                new() { Value = nameof(DeploymentMode.Deploy), Label = "Deploy (source-wins)" },
                new() { Value = nameof(DeploymentMode.Seed), Label = "Seed (field-level merge)" }
            }
        },
```

with:

```
        nameof(PredicateEditModel.Mode) => new Select
        {
            // Phase 41 D-11: clean labels (no parens). Explanatory copy lives on the Mode
            // [ConfigurableProperty] hint per D-12 (Plan 41-03 lands the hint copy + string-Mode
            // model migration).
            SortOrder = OrderBy.Default,
            Options = new List<ListOption>
            {
                new() { Value = nameof(DeploymentMode.Deploy), Label = "Deploy" },
                new() { Value = nameof(DeploymentMode.Seed), Label = "Seed" }
            }
        },
```

**Critical NON-changes:**
- The `Value = nameof(DeploymentMode.Deploy)` / `Value = nameof(DeploymentMode.Seed)` literals are PRESERVED. Plan 41-03 will migrate `PredicateEditModel.Mode` to `string` — at that point the Value strings will round-trip cleanly into `Enum.Parse<DeploymentMode>(...)`. Changing Value here would create a stale state where Plan 41-03's model migration breaks the round-trip.
- Do NOT change anything else in `PredicateEditScreen.cs`. The other Mode-related fixes (D-12 hint, D-13 model type) ship in Plan 41-03 against `PredicateEditModel.cs`, `SavePredicateCommand.cs`, and `PredicateByIndexQuery.cs` — `PredicateEditScreen.cs` body stays as-is after this label edit.
  </action>
  <verify>
    <automated>cd C:\VibeCode\DynamicWeb.Serializer && dotnet build DynamicWeb.Serializer.sln</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "(source-wins)" src/DynamicWeb.Serializer/AdminUI/Screens/PredicateEditScreen.cs` returns 0
    - `grep -c "(field-level merge)" src/DynamicWeb.Serializer/AdminUI/Screens/PredicateEditScreen.cs` returns 0
    - `grep -nE 'Label = "Deploy"' src/DynamicWeb.Serializer/AdminUI/Screens/PredicateEditScreen.cs` finds exactly 1 match (the new Deploy label)
    - `grep -nE 'Label = "Seed"' src/DynamicWeb.Serializer/AdminUI/Screens/PredicateEditScreen.cs` finds exactly 1 match (the new Seed label)
    - `grep -c 'Value = nameof(DeploymentMode.Deploy)' src/DynamicWeb.Serializer/AdminUI/Screens/PredicateEditScreen.cs` returns 1 (Value PRESERVED)
    - `grep -c 'Value = nameof(DeploymentMode.Seed)' src/DynamicWeb.Serializer/AdminUI/Screens/PredicateEditScreen.cs` returns 1 (Value PRESERVED)
    - `dotnet build DynamicWeb.Serializer.sln` exits 0
    - `dotnet test --filter "FullyQualifiedName~PredicateCommandTests.PredicateEditScreen_ModeOptions_HaveCleanLabels_PostPhase41" --no-build` exits 0 (the 41-01 RED test turns GREEN)
  </acceptance_criteria>
  <done>One file modified, two label literals replaced. Value strings unchanged. The PredicateEditScreen_ModeOptions_HaveCleanLabels fact from 41-01 turns GREEN. The model-type-mismatch facts (`ModeProperty_IsString_NotEnum`, `ModeProperty_HasHint_WithExplanatoryCopy`, the round-trip facts) STAY RED — Plan 41-03 closes those.</done>
</task>

<task type="auto">
  <name>Task 3: Document intentional empty excludeFieldsByItemType in baseline doc (D-03 + D-04)</name>
  <files>docs/baselines/Swift2.2-baseline.md</files>
  <read_first>
    - docs/baselines/Swift2.2-baseline.md (full file — confirm the existing `## Phase 40 update (2026-04-28)` paragraph at lines 64-68; new section slots in nearby with consistent heading style)
    - .planning/phases/41-admin-ui-polish/41-CONTEXT.md (D-03 + D-04 lock — investigate, then document if empty is intentional)
    - .planning/phases/41-admin-ui-polish/41-RESEARCH.md lines 80, 415-423 (research finding: pre-Phase-40 had `excludeFieldsByItemType: {}` (empty); post-Phase-40 has it absent because `ConfigWriter` omits empty dicts via `WhenWritingNull`. Net intent unchanged — document, don't restore.)
    - src/DynamicWeb.Serializer/Configuration/ConfigWriter.cs (read just enough to confirm WhenWritingNull behavior — should be referenced in the doc by line citation)
    - src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json (confirm `excludeXmlElementsByType` is present with content; `excludeFieldsByItemType` is absent — research already verified this)
  </read_first>
  <action>
**Step 1 — Confirm research findings via grep:**

Run these three commands to lock the audit baseline before writing:

```
grep -nE 'excludeFieldsByItemType|excludeXmlElementsByType' src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json | head -5
git show c5d9a8c~:src/DynamicWeb.Serializer/Configuration/swift2.2-baseline.json 2>/dev/null | grep -E 'excludeFieldsByItemType|excludeXmlElementsByType' | head -5
grep -n 'WhenWritingNull' src/DynamicWeb.Serializer/Configuration/ConfigWriter.cs | head -3
```

Expected outcomes (from RESEARCH.md):
- Current `swift2.2-combined.json`: `excludeXmlElementsByType` present with 21 elements for `eCom_CartV2`; `excludeFieldsByItemType` NOT present (omitted by ConfigWriter).
- Pre-Phase-40 `swift2.2-baseline.json` at commit `c5d9a8c~`: `excludeFieldsByItemType: {}` (explicit empty dict) and `excludeXmlElementsByType` with empty/sparse arrays.
- `ConfigWriter.cs`: `WhenWritingNull` logic at lines ~35-36 omits empty dicts.

If any of these expectations DOES NOT match (e.g., `excludeFieldsByItemType` is actually present in `swift2.2-combined.json` with content), STOP and escalate to orchestrator — the audit conclusion changes from "document empty" to "investigate why content was lost" and the action below is wrong. Otherwise proceed.

**Step 2 — Append a new section to `docs/baselines/Swift2.2-baseline.md`:**

Insert this section AFTER the existing `## Per-predicate mode` block (find the end of that block — it's the section starting at "Every predicate in `swift2.2-combined.json` declares its own `mode`..."). The new section heading is `## Exclusion sections`. Verbatim content:

```markdown
## Exclusion sections

The post-Phase-40 baseline ships two top-level mode-agnostic exclusion dicts on
`swift2.2-combined.json`: `excludeFieldsByItemType` and `excludeXmlElementsByType`.
Their populated state is intentional and documented here so future readers do not
mistake their shape for an oversight.

### `excludeFieldsByItemType` — empty by design

The post-Phase-40 baseline does NOT contain an `excludeFieldsByItemType` key. This
is intentional, not a regression introduced during the Phase 40 flat-shape rewrite.

Per-ItemType field exclusions can be added at any time via the admin UI
(Settings → System → Developer → Serialize → Item Type Excludes → choose a type),
but the Swift 2.2 baseline ships no such overrides. Every Swift 2.2 ItemType is
serialized with all its fields by default.

The pre-Phase-40 baseline (`swift2.2-baseline.json` at commit `c5d9a8c~`, deleted
during Phase 40) carried an explicit empty dict `"excludeFieldsByItemType": {}`.
The new `ConfigWriter.Save` path omits empty dicts via the `WhenWritingNull` path
in `src/DynamicWeb.Serializer/Configuration/ConfigWriter.cs` (~lines 35-36), so the
key disappears from the JSON file the next time the config is saved. The semantic
behavior — no per-ItemType exclusions — is unchanged. There is nothing to restore.

### `excludeXmlElementsByType` — expanded during Phase 40

The post-Phase-40 baseline carries a populated `excludeXmlElementsByType` dict
covering Phase 40's curated set of XML payload types. Notably, `eCom_CartV2`
ships with 21 excluded element names. This dict was *expanded* during Phase 40
(commit `d57d474`), not lost — content was added on top of the pre-Phase-40
sparse keys, not removed.

Per Phase 41 D-03: this section was added 2026-05-01 after a content audit
against the pre-Phase-40 baseline confirmed both shapes are intentional. The
audit lives in `.planning/phases/41-admin-ui-polish/41-RESEARCH.md` (the D-03
finding section).
```

**Critical formatting:**
- Heading level `## Exclusion sections` matches the rest of the doc (`## Purpose`, `## The three-bucket split`, `## Per-predicate mode`).
- Sub-headings use `### `.
- Code-fenced filenames use single backticks (matches existing doc style).
- No emojis. No trailing whitespace.
  </action>
  <verify>
    <automated>cd C:\VibeCode\DynamicWeb.Serializer && grep -c "excludeFieldsByItemType" docs/baselines/Swift2.2-baseline.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Exclusion sections" docs/baselines/Swift2.2-baseline.md` returns 1
    - `grep -c "excludeFieldsByItemType" docs/baselines/Swift2.2-baseline.md` returns at least 3 (heading reference + key explanation + ConfigWriter explanation)
    - `grep -c "excludeXmlElementsByType" docs/baselines/Swift2.2-baseline.md` returns at least 2
    - `grep -c "empty by design" docs/baselines/Swift2.2-baseline.md` returns 1
    - `grep -c "c5d9a8c" docs/baselines/Swift2.2-baseline.md` returns 1 (cite the deletion commit per RESEARCH.md)
    - `grep -c "Phase 41 D-03" docs/baselines/Swift2.2-baseline.md` returns 1 (traceability)
    - The doc has no broken markdown — `grep -E "^####|^#####" docs/baselines/Swift2.2-baseline.md` returns at most pre-existing matches (the new section uses only `##` and `###`)
  </acceptance_criteria>
  <done>One section appended to docs/baselines/Swift2.2-baseline.md. Section explains the post-Phase-40 shape of both exclusion dicts, cites the `c5d9a8c~` deletion + ConfigWriter `WhenWritingNull` behavior, and references Phase 41 D-03 for traceability. No content restoration to swift2.2-combined.json — D-03 is closed via documentation per the audit conclusion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI strings | Hardcoded compile-time literals; no user input flows in. |
| Doc file | Static markdown; no runtime parsing or rendering risk. |

## STRIDE Threat Register

This plan ships rename-only and docs-only changes. The fixes do not touch validation, parsing, persistence, or any control-flow path. Threats are inherited from the surrounding code with no new attack surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | Information Disclosure | Renamed tree node display string | accept | "Item Type Excludes" is hardcoded; no user input. CoreUI renderer encodes attributes. No mitigation needed. |
| T-41-02-02 | Information Disclosure | Mode dropdown option Labels | accept | "Deploy" / "Seed" are hardcoded; CoreUI Select renderer encodes. No mitigation needed. |
| T-41-02-03 | Tampering | Mode dropdown Value strings preserved | mitigate (preserve) | The Value strings remain `nameof(DeploymentMode.Deploy)` / `nameof(DeploymentMode.Seed)`. Plan 41-03's `Enum.Parse<DeploymentMode>(... ignoreCase: true)` gate will reject non-enum-name strings. This plan must NOT change Value strings — doing so would break the Plan 41-03 round-trip. |

Cross-reference: Plan 41-01 ships the test that proves T-41-02-03's invariant once Plan 41-03 lands.
</threat_model>

<verification>
After all 3 tasks complete:

```
cd C:\VibeCode\DynamicWeb.Serializer
dotnet build DynamicWeb.Serializer.sln
dotnet test tests/DynamicWeb.Serializer.Tests/DynamicWeb.Serializer.Tests.csproj --filter "FullyQualifiedName~AdminUI" --no-build
```

Compare to the 41-01 baseline:
- 4 facts that were RED in 41-01 should turn GREEN here:
  1. SerializerSettingsNodeProviderModeTreeTests.GetSubNodes_UnderSerializeNode_ItemTypesNode_DisplayName_IsItemTypeExcludes
  2. ItemTypeEditScreenTests.GetScreenName_WithSystemName_StartsWithItemTypeExcludes
  3. ItemTypeEditScreenTests.GetScreenName_NoModel_IsItemTypeExcludes
  4. PredicateCommandTests.PredicateEditScreen_ModeOptions_HaveCleanLabels_PostPhase41
- The remaining 41-01 RED facts (D-05/D-06/D-12/D-13 surface) STAY RED — Plan 41-03 closes those.
- Pre-existing AdminUI facts must not regress.
</verification>

<success_criteria>
1. 5 files modified: 3 source (rename) + 1 source (label) + 1 docs.
2. `dotnet build DynamicWeb.Serializer.sln` exits 0.
3. The 4 specifically-named RED facts from 41-01 turn GREEN.
4. No pre-existing AdminUI fact regresses.
5. The const `ItemTypesNodeId = "Serializer_ItemTypes"` is verifiably preserved (D-02).
6. The Mode Select Value strings (`nameof(DeploymentMode.*)`) are preserved (Plan 41-03 dependency).
7. CreateFieldSelector in ItemTypeEditScreen is verifiably untouched (Plan 41-03 owns it).
8. docs/baselines/Swift2.2-baseline.md has the new "Exclusion sections" section with the documented shape.
</success_criteria>

<output>
After completion, create `.planning/phases/41-admin-ui-polish/41-02-SUMMARY.md` with:
- Files modified (5 paths) + line ranges touched
- The 4 RED→GREEN transitions confirmed by `dotnet test` output
- Confirmation that ItemTypesNodeId const + DeploymentMode Value literals are preserved
- The grep audit results from Task 3 step 1 (so Plan 41-03 inherits the audit ground truth)
- Any deviations or issues encountered
</output>
