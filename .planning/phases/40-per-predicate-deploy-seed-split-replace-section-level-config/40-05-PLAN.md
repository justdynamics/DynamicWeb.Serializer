---
phase: 40-per-predicate-deploy-seed-split-replace-section-level-config
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Full solution builds with zero CS errors"
    - "Full test suite passes with zero failures"
    - "No file under src/ or tests/ references the removed legacy surface (config.Deploy, config.Seed, GetMode, ModeConfig, new ModeConfig)"
    - "swift2.2-combined.json round-trips through ConfigLoader.Load + ConfigWriter.Save with Deploy/Seed counts preserved"
    - "Three example JSONs (demo-sync.json, ecommerce-predicates-example.json, full-sync-example.json) are in the flat shape — every predicate has 'mode'; no top-level 'conflictStrategy' key"
    - "Legacy-shape config files are rejected at load time with a Phase-40-citing error"
  artifacts:
    - path: ".planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-VERIFICATION.md"
      provides: "Evidence-based phase-completion record"
      contains: "PASSED"
  key_links:
    - from: "Plan 05 verification"
      to: "Plans 01-04 deliverables"
      via: "End-to-end build + test gate"
      pattern: "dotnet build.*0 Error\\(s\\)"
---

<objective>
Phase-completion gate. Run the full solution build + test suite. Run a curated set of grep-based regression checks proving the legacy Deploy/Seed surface is gone everywhere it could regress to. Capture the evidence in a VERIFICATION.md so the phase summary has machine-checkable proof of completion.

Purpose: Plans 01-04 each verify their own scope, but no single plan ran the FULL solution-level gate. Plan 05 closes that — it's the equivalent of Phase 38-05's "final live E2E" task, scoped to the config-shape change that does not require a live DB roundtrip (Plan 04's checkpoint already covered the live-host smoke test).

Output: 40-VERIFICATION.md with build output, test output, regression-grep results, all under PASS / FAIL disposition. If any check fails, this plan does NOT mark itself complete — it loops the failure back to the planner for revision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-01-SUMMARY.md
@.planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-02-SUMMARY.md
@.planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-03-SUMMARY.md
@.planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Solution-wide build + full test suite + regression greps</name>
  <read_first>
    - .planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-01-SUMMARY.md
    - .planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-02-SUMMARY.md
    - .planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-03-SUMMARY.md
    - .planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-04-SUMMARY.md
  </read_first>
  <action>
1. **Run the full solution build:**
   ```
   cd C:/VibeCode/DynamicWeb.Serializer
   dotnet build DynamicWeb.Serializer.sln 2>&1 | tail -30
   ```
   Capture the last 30 lines of output. Expected: `Build succeeded` line with `0 Error(s)`. Warning count is informational; not a fail criterion.

2. **Run the full test suite:**
   ```
   dotnet test tests/DynamicWeb.Serializer.Tests/DynamicWeb.Serializer.Tests.csproj --no-build --logger "console;verbosity=normal" 2>&1 | tail -30
   ```
   Capture: total test count, passed count, failed count. Expected: failed=0.

3. **Run the regression grep battery** (these prove the legacy surface is gone everywhere):
   ```
   echo "=== R-01: ModeConfig type referenced anywhere in src/tests ==="
   grep -rn "ModeConfig\b" src/ tests/ --include="*.cs" || echo "PASS: zero matches"

   echo "=== R-02: config.Deploy or config.Seed property access ==="
   grep -rn "config\.Deploy\|config\.Seed\|_configuration\.Deploy\|_configuration\.Seed" src/ tests/ --include="*.cs" || echo "PASS: zero matches"

   echo "=== R-03: GetMode method calls ==="
   grep -rn "\.GetMode(" src/ tests/ --include="*.cs" || echo "PASS: zero matches"

   echo "=== R-04: 'new ModeConfig' construction ==="
   grep -rn "new ModeConfig" src/ tests/ --include="*.cs" || echo "PASS: zero matches"

   echo "=== R-05: legacy shape JSON in test fixtures or baseline ==="
   grep -rE '^\s*"deploy":\s*\{|^\s*"seed":\s*\{' src/DynamicWeb.Serializer/Configuration/*.json tests/DynamicWeb.Serializer.Tests/ --include="*.json" --include="*.cs" || echo "PASS: zero matches"

   echo "=== R-06: every predicate in the swift2.2 baseline has a mode field ==="
   PRED_COUNT=$(grep -c "\"name\":" src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json)
   MODE_COUNT=$(grep -c "\"mode\":" src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json)
   if [ "$PRED_COUNT" -eq "$MODE_COUNT" ]; then
     echo "PASS: predicate count $PRED_COUNT == mode count $MODE_COUNT"
   else
     echo "FAIL: predicate count $PRED_COUNT != mode count $MODE_COUNT"
   fi

   echo "=== R-07: ConfigLoader has a 'Legacy section-level shape' rejection message ==="
   grep -c "Legacy section-level shape" src/DynamicWeb.Serializer/Configuration/ConfigLoader.cs

   echo "=== R-08: ItemTypePerModeTests / XmlTypePerModeTests deleted ==="
   if [ ! -f tests/DynamicWeb.Serializer.Tests/AdminUI/ItemTypePerModeTests.cs ] && [ ! -f tests/DynamicWeb.Serializer.Tests/AdminUI/XmlTypePerModeTests.cs ]; then
     echo "PASS: both files deleted"
   else
     echo "FAIL: at least one obsolete test file still present"
   fi

   echo "=== R-09: PredicateEditScreen renders Mode editor ==="
   grep -c "EditorFor(m => m.Mode)" src/DynamicWeb.Serializer/AdminUI/Screens/PredicateEditScreen.cs

   echo "=== R-10: PredicateListScreen renders Mode column ==="
   grep -c "ModeDisplay\|m.Mode" src/DynamicWeb.Serializer/AdminUI/Screens/PredicateListScreen.cs

   echo "=== R-11: example JSONs (demo-sync, ecommerce-predicates-example, full-sync-example) — every predicate has mode ==="
   FAIL_R11=0
   for f in src/DynamicWeb.Serializer/Configuration/demo-sync.json src/DynamicWeb.Serializer/Configuration/ecommerce-predicates-example.json src/DynamicWeb.Serializer/Configuration/full-sync-example.json; do
     NAME=$(grep -c "\"name\":" $f)
     MODE=$(grep -c "\"mode\":" $f)
     if [ "$NAME" -ne "$MODE" ]; then
       echo "FAIL R-11: $f name($NAME) != mode($MODE)"
       FAIL_R11=1
     fi
   done
   if [ "$FAIL_R11" -eq 0 ]; then echo "PASS: all 3 example JSONs have 1:1 name:mode pairing"; fi

   echo "=== R-12: example JSONs do not carry top-level conflictStrategy ==="
   if grep -lE '^\s*"conflictStrategy"' src/DynamicWeb.Serializer/Configuration/demo-sync.json src/DynamicWeb.Serializer/Configuration/ecommerce-predicates-example.json src/DynamicWeb.Serializer/Configuration/full-sync-example.json 2>/dev/null; then
     echo "FAIL: at least one example JSON still has top-level conflictStrategy"
   else
     echo "PASS: zero examples carry conflictStrategy"
   fi

   echo "=== R-13: ScanXmlTypesCommand has no Mode property and writes to top-level dict ==="
   if grep -q "public DeploymentMode Mode" src/DynamicWeb.Serializer/AdminUI/Commands/ScanXmlTypesCommand.cs; then
     echo "FAIL: ScanXmlTypesCommand still has Mode property"
   else
     echo "PASS: Mode property removed"
   fi
   grep -c "config.ExcludeXmlElementsByType" src/DynamicWeb.Serializer/AdminUI/Commands/ScanXmlTypesCommand.cs
   ```
   R-07, R-09, R-10, R-13 (second grep) must each return ≥ 1; R-01..R-05, R-08, R-11, R-12, R-13 (first check) must each report `PASS:`; R-06 must report `PASS:` with matching counts.

4. **Write** `.planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-VERIFICATION.md` containing:
   ```markdown
   # Phase 40 Verification

   **Date:** {YYYY-MM-DD}
   **Disposition:** PASSED | FAILED

   ## Build
   ```
   {paste build output tail}
   ```

   ## Tests
   ```
   {paste test output tail}
   ```
   - Total: N
   - Passed: N
   - Failed: 0

   ## Regression greps
   | Check | Description | Result |
   |-------|-------------|--------|
   | R-01 | No ModeConfig type referenced | PASS / FAIL |
   | R-02 | No config.Deploy / config.Seed property access | PASS / FAIL |
   | R-03 | No GetMode method calls | PASS / FAIL |
   | R-04 | No 'new ModeConfig' constructions | PASS / FAIL |
   | R-05 | No legacy JSON shape in fixtures or baseline | PASS / FAIL |
   | R-06 | Baseline predicate count == mode-key count | PASS / FAIL |
   | R-07 | ConfigLoader has Phase-40 rejection message | PASS / FAIL ({count} matches) |
   | R-08 | ItemTypePerModeTests / XmlTypePerModeTests deleted | PASS / FAIL |
   | R-09 | PredicateEditScreen renders Mode editor | PASS / FAIL ({count} matches) |
   | R-10 | PredicateListScreen renders Mode column | PASS / FAIL ({count} matches) |
   | R-11 | All 3 example JSONs have 1:1 name:mode pairing | PASS / FAIL |
   | R-12 | No example JSON carries top-level conflictStrategy | PASS / FAIL |
   | R-13 | ScanXmlTypesCommand has no Mode property; writes to top-level dict | PASS / FAIL |

   ## Disposition

   {If all checks PASS} → Phase 40 CLOSED. swift2.2-combined.json shipped in flat shape; admin UI collapsed to single tree; legacy section shape rejected at load with clear error.

   {If any check FAILS} → Phase 40 HELD. List the failing check IDs and a brief diagnosis of which Plan (01-04) needs revision.
   ```

5. If ANY check fails: do NOT write `Disposition: PASSED`. Write `FAILED` and a clear-text description of which checks failed and which Plan needs revision. Do NOT loop or try to fix things in this plan — the executor's job here is to RUN the checks and RECORD the result. Failures route back to the planner via the orchestrator.

6. If ALL checks pass: write `Disposition: PASSED`, commit the VERIFICATION.md, and return success to the orchestrator.
  </action>
  <verify>
    <automated>cd C:/VibeCode/DynamicWeb.Serializer &amp;&amp; test -f .planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-VERIFICATION.md &amp;&amp; grep -E "Disposition:\s*PASSED|Disposition:\s*FAILED" .planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-VERIFICATION.md</automated>
    Expected: at least one match. The disposition is the gate.
  </verify>
  <done>
    - `dotnet build DynamicWeb.Serializer.sln` returns exit 0 with `0 Error(s)`
    - `dotnet test` reports `Failed: 0`
    - All 13 regression greps (R-01..R-13) report PASS
    - 40-VERIFICATION.md exists and contains `Disposition: PASSED`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plans 01-04 deliverables → Plan 05 gate | Aggregated artefacts cross into a single pass/fail check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05-01 | Tampering | Verification record falsifies a check | mitigate | Each grep is reproducible. The VERIFICATION.md captures the actual command lines + their actual output. Auditors can re-run any line to confirm. |
| T-40-05-02 | Information disclosure | VERIFICATION.md leaks customer-edited content | accept | Test output may include source paths; no DB content or credentials surface here. Standard project hygiene. |
| T-40-05-03 | Repudiation | Phase claimed PASSED with stale build artefacts | mitigate | The `dotnet test --no-build` runs against the most recent build; the build itself runs first in step 1 of Task 1. Both outputs are pasted into VERIFICATION.md so the timestamps line up. |
| T-40-05-04 | Tampering | Example JSONs regress to legacy shape between Plan 04 ship and Plan 05 gate | mitigate | R-11 + R-12 + R-13 explicitly grep the three example files and ScanXmlTypesCommand. Any contributor who reverts those changes (intentionally or via merge accident) trips the gate. |
</threat_model>

<verification>
The plan IS the verification — Task 1 captures everything. Plan-level success criterion: VERIFICATION.md exists with `Disposition: PASSED`.
</verification>

<success_criteria>
- 40-VERIFICATION.md created with all 13 regression grep results
- All checks PASS
- Full solution builds clean
- Full test suite green
- Disposition: PASSED is recorded
</success_criteria>

<output>
After completion, create `.planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-05-SUMMARY.md` with:
- Final test count delta vs. pre-Phase-40 baseline (which was at 620 per Phase 37-06's STATE.md log)
- Total source files changed across the phase (sum across Plans 01-04 file counts)
- Commit hash of the phase-closing commit
- Reference to 40-VERIFICATION.md for the evidence trail
</output>
