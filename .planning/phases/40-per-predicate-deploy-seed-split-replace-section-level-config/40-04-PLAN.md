---
phase: 40-per-predicate-deploy-seed-split-replace-section-level-config
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json
  - src/DynamicWeb.Serializer/Configuration/demo-sync.json
  - src/DynamicWeb.Serializer/Configuration/ecommerce-predicates-example.json
  - src/DynamicWeb.Serializer/Configuration/full-sync-example.json
  - docs/baselines/Swift2.2-baseline.md
  - docs/configuration.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "swift2.2-combined.json is in the new flat shape — single top-level 'predicates' array, each entry has a 'mode' key with value 'Deploy' or 'Seed'"
    - "Top-level 'deploy' and 'seed' object keys do not appear anywhere in swift2.2-combined.json"
    - "Top-level 'excludeXmlElementsByType' key is hoisted out of the (former) deploy section to the top level (D-04)"
    - "swift2.2-combined.json parses without errors via ConfigLoader.Load when identifier validation is bypassed (identifierValidator: null); mode counts and predicate-named lookups match expected baseline (17 Deploy + 9 Seed = 26 total predicates; EcomShops=Deploy; EcomGroups=Seed)"
    - "demo-sync.json, ecommerce-predicates-example.json, full-sync-example.json are rewritten in the flat shape — every predicate has explicit 'mode': 'Deploy', the top-level 'conflictStrategy' key is dropped — so users copying these examples produce ConfigLoader-acceptable input"
    - "docs/baselines/Swift2.2-baseline.md describes the per-predicate Mode field and the new flat shape; legacy Deploy/Seed section wording is updated"
    - "docs/configuration.md reflects the new flat shape with at least one example showing predicates with explicit 'mode' values"
  artifacts:
    - path: "src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json"
      provides: "Canonical Phase 40 baseline config"
      contains: "\"mode\": \"Deploy\""
    - path: "src/DynamicWeb.Serializer/Configuration/demo-sync.json"
      provides: "Demo example in flat shape"
      contains: "\"mode\": \"Deploy\""
    - path: "src/DynamicWeb.Serializer/Configuration/ecommerce-predicates-example.json"
      provides: "Ecommerce predicates example in flat shape"
      contains: "\"mode\": \"Deploy\""
    - path: "src/DynamicWeb.Serializer/Configuration/full-sync-example.json"
      provides: "Full-sync example in flat shape"
      contains: "\"mode\": \"Deploy\""
    - path: "docs/baselines/Swift2.2-baseline.md"
      provides: "Reasoning doc updated for flat shape"
      contains: "per-predicate"
    - path: "docs/configuration.md"
      provides: "User-facing config reference"
      contains: "mode"
  key_links:
    - from: "swift2.2-combined.json"
      to: "ConfigLoader"
      via: "Load round-trip — every predicate parses with its own Mode"
      pattern: "\"mode\": \"(Deploy|Seed)\""
    - from: "Swift2.2-baseline.md"
      to: "swift2.2-combined.json"
      via: "Doc references the new shape"
      pattern: "Phase 40"
    - from: "demo-sync.json / ecommerce-predicates-example.json / full-sync-example.json"
      to: "ConfigLoader"
      via: "Examples must remain copy-paste-able into ConfigLoader without rejection"
      pattern: "\"mode\": \""
---

<objective>
Rewrite the canonical baseline config (`swift2.2-combined.json`) AND the three example/documentation configs (`demo-sync.json`, `ecommerce-predicates-example.json`, `full-sync-example.json`) into the Phase 40 flat shape. Update the two reasoning docs that reference the legacy section-level shape. Includes a checkpoint for the human operator to confirm the live Swift 2.2 host loads the new baseline cleanly before declaring the phase complete.

Purpose: D-05 — `swift2.2-combined.json` is THE canonical baseline (per project memory `project_swift22_baseline_combined.md`). It must ship in the new shape. The three example/documentation configs are user-facing copy-paste artefacts; if they retain the legacy shape (or even just retain `"conflictStrategy"`/missing-`"mode"` predicate entries), a user copying them produces a config that ConfigLoader hard-rejects per D-03. Per checker Warning #5 (no-backcompat is also no-trap-for-the-user), they get the same rewrite. Existing customers' (none yet — beta) configs are not migrated; the live tooling loads the new baseline only.

Output: Four JSON files rewritten, two markdown docs updated, one human-verify checkpoint that the rewritten baseline loads cleanly via `ConfigLoader.Load` against the live DW DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-01-SUMMARY.md
@CLAUDE.md
@src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json
@src/DynamicWeb.Serializer/Configuration/demo-sync.json
@src/DynamicWeb.Serializer/Configuration/ecommerce-predicates-example.json
@src/DynamicWeb.Serializer/Configuration/full-sync-example.json
@docs/baselines/Swift2.2-baseline.md
@docs/configuration.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite swift2.2-combined.json into Phase 40 flat shape</name>
  <read_first>
    - src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json (the current 297-line file with deploy/seed sections)
  </read_first>
  <action>
1. **Replace** the entire contents of `src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json` with the flat-shape equivalent. The mapping rules:
   - Every predicate currently in `deploy.predicates[]` becomes a top-level `predicates[]` entry with `"mode": "Deploy"` added (insert as the second key after `"name"` for readability).
   - Every predicate currently in `seed.predicates[]` becomes a top-level `predicates[]` entry with `"mode": "Seed"` added.
   - The `deploy.excludeXmlElementsByType` dictionary moves verbatim to a top-level `excludeXmlElementsByType` key.
   - The two `outputSubfolder` values become `deployOutputSubfolder` and `seedOutputSubfolder` at the top level (both are `"deploy"` and `"seed"` respectively in the current file, so the values are identical — only the key location moves).
   - The two `conflictStrategy` values are DROPPED — Phase 40 D-02 hardcodes them per mode (Deploy=source-wins, Seed=destination-wins). Removing them from the JSON has no behavior impact and the on-disk file is shorter.
   - Order: Deploy predicates first, then Seed predicates (preserves the current section ordering for diff readability).

2. The exact target structure (preserve every existing field on every predicate; only ADD `mode` and HOIST `excludeXmlElementsByType`):
   ```json
   {
     "outputDirectory": "Serializer",
     "logLevel": "info",
     "dryRun": false,
     "deployOutputSubfolder": "deploy",
     "seedOutputSubfolder": "seed",
     "excludeXmlElementsByType": {
       "Dynamicweb.Ecommerce.Frontend.UrlHandling.ShopUrlDataProvider, Dynamicweb.Ecommerce": [],
       "eCom_CartV2": [
         "EmptyCartRedirectPage",
         "CustomerAcceptedErrorMessage",
         "StockStatusErrorMessage",
         "ErrorEmtyUsername",
         "ErrorUsernameTaken",
         "ErrorEmtyPassword",
         "ErrorPasswordsNotMatch",
         "ErrorPasswordLength",
         "ErrorIllegalPasswordCharacters",
         "ErrorMessage_VALIDATION1",
         "ErrorMessage_VALIDATION2",
         "ErrorMessage_VALIDATION4",
         "Mail1SenderName",
         "Mail1SenderEmail",
         "Mail1Subject",
         "Mail1Recipient",
         "CreateNewUserGroupsHidden",
         "DefaultPaymentId",
         "DefaultShippingId",
         "PaymetTypes",
         "DeliveryTypes"
       ],
       "eCom_Catalog": [],
       "eCom_ContextOrderRenderer": [],
       "eCom_CustomerExperienceCenter": [
         "ShoppingCartLink"
       ],
       "eCom_CustomerExperienceCenterFavorites": [],
       "eCom_CustomerExperienceCenterSavedCards": [],
       "eCom_ProductCatalog": [],
       "QueryPublisher": [],
       "UserAddresses": [],
       "UserAuthentication": [
         "OneTimeCodeSenderName",
         "OneTimeCodeSenderEmail",
         "OneTimeCodeEmailSubject",
         "MagicLinkSenderName",
         "MagicLinkSenderEmail",
         "MagicLinkEmailSubject"
       ],
       "UserChangePassword": [],
       "UserCreate": [
         "SenderName",
         "SenderEmail",
         "EmailSubject"
       ],
       "UserEdit": [
         "SenderName",
         "SenderEmail",
         "EmailSubject"
       ],
       "UserForgotPassword": [
         "SenderName",
         "SenderEmail",
         "EmailSubject"
       ],
       "UserGroups": [],
       "UserView": [
         "SenderName",
         "SenderEmail",
         "EmailSubject"
       ]
     },
     "predicates": [
       {
         "name": "Content - Swift 2 (full baseline as shipped)",
         "mode": "Deploy",
         "providerType": "Content",
         "areaId": 3,
         "path": "/",
         "excludes": [],
         "excludeFields": [
           "AreaDomain",
           "AreaDomainLock",
           "AreaNoindex",
           "AreaNofollow",
           "AreaRobotsTxt",
           "AreaRobotsTxtIncludeSitemap",
           "GoogleTagManagerID",
           "Favicon",
           "AppleTouchIcon",
           "FacebookAppId",
           "Google_APIKey",
           "Google_Site_Verification",
           "P_domain_verify",
           "TwitterSite",
           "MetaSiteName",
           "MetaImage",
           "MetaImageALT"
         ],
         "excludeXmlElements": [],
         "excludeAreaColumns": [
           "AreaCdnHost",
           "AreaCookieWarningTemplate",
           "AreaFrontpage",
           "AreaEcomShopId",
           "AreaEcomCountryCode",
           "AreaEcomLanguageId",
           "AreaEcomCurrencyId",
           "AreaStockLocationID",
           "AreaCreatedDate",
           "AreaUpdatedDate"
         ]
       },
       { "name": "EcomCountries", "mode": "Deploy", "providerType": "SqlTable", "table": "EcomCountries",
         "serviceCaches": [
           "Dynamicweb.Ecommerce.International.CountryRelationService",
           "Dynamicweb.Ecommerce.International.CountryService"
         ]
       },
       { "name": "EcomCountryText", "mode": "Deploy", "providerType": "SqlTable", "table": "EcomCountryText",
         "serviceCaches": [ "Dynamicweb.Ecommerce.International.CountryService" ] },
       { "name": "EcomCurrencies", "mode": "Deploy", "providerType": "SqlTable", "table": "EcomCurrencies",
         "serviceCaches": [ "Dynamicweb.Ecommerce.International.CurrencyService" ] },
       { "name": "EcomLanguages", "mode": "Deploy", "providerType": "SqlTable", "table": "EcomLanguages",
         "serviceCaches": [ "Dynamicweb.Ecommerce.International.LanguageService" ] },
       { "name": "EcomVatGroups", "mode": "Deploy", "providerType": "SqlTable", "table": "EcomVatGroups",
         "nameColumn": "VatGroupName",
         "serviceCaches": [ "Dynamicweb.Ecommerce.International.VatGroupService" ] },
       { "name": "EcomVatCountryRelations", "mode": "Deploy", "providerType": "SqlTable", "table": "EcomVatCountryRelations",
         "serviceCaches": [ "Dynamicweb.Ecommerce.International.VatGroupCountryRelationService" ] },
       { "name": "EcomShops", "mode": "Deploy", "providerType": "SqlTable", "table": "EcomShops" },
       { "name": "EcomShopLanguageRelation", "mode": "Deploy", "providerType": "SqlTable", "table": "EcomShopLanguageRelation" },
       { "name": "EcomShopGroupRelation", "mode": "Deploy", "providerType": "SqlTable", "table": "EcomShopGroupRelation" },
       { "name": "EcomPayments", "mode": "Deploy", "providerType": "SqlTable", "table": "EcomPayments",
         "nameColumn": "PaymentName",
         "xmlColumns": [ "PaymentGatewayParameters", "PaymentCheckoutParameters" ],
         "serviceCaches": [ "Dynamicweb.Ecommerce.Orders.PaymentService" ] },
       { "name": "EcomShippings", "mode": "Deploy", "providerType": "SqlTable", "table": "EcomShippings",
         "nameColumn": "ShippingName",
         "xmlColumns": [ "ShippingServiceParameters" ],
         "serviceCaches": [ "Dynamicweb.Ecommerce.Orders.ShippingService" ] },
       { "name": "EcomMethodCountryRelation", "mode": "Deploy", "providerType": "SqlTable", "table": "EcomMethodCountryRelation" },
       { "name": "EcomOrderFlow", "mode": "Deploy", "providerType": "SqlTable", "table": "EcomOrderFlow", "nameColumn": "OrderFlowName" },
       { "name": "EcomOrderStates", "mode": "Deploy", "providerType": "SqlTable", "table": "EcomOrderStates", "nameColumn": "OrderStateName" },
       { "name": "EcomOrderStateRules", "mode": "Deploy", "providerType": "SqlTable", "table": "EcomOrderStateRules" },
       { "name": "UrlPath", "mode": "Deploy", "providerType": "SqlTable", "table": "UrlPath", "resolveLinksInColumns": [ "UrlPathRedirect" ] },

       { "name": "Content - Swift 2 (seed defaults for customer-tunable fields)",
         "mode": "Seed",
         "providerType": "Content",
         "areaId": 3,
         "path": "/",
         "excludes": [],
         "excludeFields": [],
         "excludeXmlElements": [],
         "excludeAreaColumns": [] },
       { "name": "EcomGroups", "mode": "Seed", "providerType": "SqlTable", "table": "EcomGroups", "nameColumn": "GroupName" },
       { "name": "EcomProducts", "mode": "Seed", "providerType": "SqlTable", "table": "EcomProducts", "nameColumn": "ProductName" },
       { "name": "EcomGroupProductRelation", "mode": "Seed", "providerType": "SqlTable", "table": "EcomGroupProductRelation" },
       { "name": "EcomVariantGroups", "mode": "Seed", "providerType": "SqlTable", "table": "EcomVariantGroups", "nameColumn": "VariantGroupName" },
       { "name": "EcomVariantsOptions", "mode": "Seed", "providerType": "SqlTable", "table": "EcomVariantsOptions", "nameColumn": "VariantOptionName" },
       { "name": "EcomVariantOptionsProductRelation", "mode": "Seed", "providerType": "SqlTable", "table": "EcomVariantOptionsProductRelation" },
       { "name": "EcomDiscount", "mode": "Seed", "providerType": "SqlTable", "table": "EcomDiscount", "nameColumn": "DiscountName" },
       { "name": "EcomDiscountTranslation", "mode": "Seed", "providerType": "SqlTable", "table": "EcomDiscountTranslation" }
     ]
   }
   ```
   The compact one-line predicate format is acceptable for the SqlTable predicates that have only 2-4 fields; keep the multi-line format for the two Content predicates and EcomPayments / EcomShippings (which have nested arrays). The existing file uses indented-multiline for everything; you may either preserve that or compact — both pass through ConfigLoader/ConfigWriter identically. Pick one and document the choice in the SUMMARY.

3. Run the round-trip test:
   ```
   cd C:/VibeCode/DynamicWeb.Serializer
   dotnet test tests/DynamicWeb.Serializer.Tests/DynamicWeb.Serializer.Tests.csproj --filter "FullyQualifiedName~ConfigLoaderTests|FullyQualifiedName~Phase40FlatConfigLoaderTests|FullyQualifiedName~ConfigWriterTests" --no-restore
   ```
   Add a NEW test in `tests/DynamicWeb.Serializer.Tests/Configuration/Swift22BaselineRoundTripTests.cs` that:
   - Loads `src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json` via `ConfigLoader.Load(path, identifierValidator: null)` (per checker Warning #7 recommended option (b): the test scopes its assertion to the new-shape JSON parse + Mode round-trip; it does NOT exercise the SqlIdentifier validator pipeline. Extending the validator allowlist with the 23 baseline SqlTable names is disproportionate to the value — that path is exercised end-to-end via the live host smoke test in Task 4, not via unit test.)
   - Asserts `config.Predicates.Count == 26` (17 Deploy + 9 Seed per the source file)
   - Asserts `config.Predicates.Count(p => p.Mode == DeploymentMode.Deploy) == 17`
   - Asserts `config.Predicates.Count(p => p.Mode == DeploymentMode.Seed) == 9`
   - Asserts `config.ExcludeXmlElementsByType.Count >= 16` (the count of keys in the dict above)
   - Asserts the predicate named `"EcomShops"` has Mode=Deploy
   - Asserts the predicate named `"EcomGroups"` has Mode=Seed

   The test does NOT inherit from `ConfigLoaderValidatorFixtureBase` because the `identifierValidator: null` overload bypasses the static validator entirely — there is no fixture state to manage. Document this scope decision in the test's class-level XML doc comment.
  </action>
  <verify>
    <automated>cd C:/VibeCode/DynamicWeb.Serializer &amp;&amp; grep -c "\"mode\": \"Deploy\"\\|\"mode\":\"Deploy\"" src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json &amp;&amp; grep -c "\"mode\": \"Seed\"\\|\"mode\":\"Seed\"" src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json &amp;&amp; ! grep -E '^\s*"deploy":\s*\{|^\s*"seed":\s*\{' src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json &amp;&amp; dotnet test tests/DynamicWeb.Serializer.Tests/DynamicWeb.Serializer.Tests.csproj --filter "FullyQualifiedName~Swift22BaselineRoundTripTests" --no-restore 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `grep -c "\"mode\":" src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json` returns exactly 26
    - `grep -c "\"mode\": \"Deploy\"\\|\"mode\":\"Deploy\"" src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json` returns exactly 17
    - `grep -c "\"mode\": \"Seed\"\\|\"mode\":\"Seed\"" src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json` returns exactly 9
    - The file contains NO line matching `^\s*"deploy":\s*\{` and NO line matching `^\s*"seed":\s*\{` (regex check via `grep -E`)
    - The new Swift22BaselineRoundTripTests passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the three example/documentation JSONs (demo-sync, ecommerce-predicates-example, full-sync-example) into Phase 40 flat shape</name>
  <read_first>
    - src/DynamicWeb.Serializer/Configuration/demo-sync.json (current 148-line file: top-level `conflictStrategy` + flat `predicates[]` with no `mode` field on any entry — the OLD pre-Phase-37 shape, NOT the Phase-37/38 deploy/seed shape. ConfigLoader will reject this post-Plan-01 because every predicate is missing the required `mode` field.)
    - src/DynamicWeb.Serializer/Configuration/ecommerce-predicates-example.json (current 271-line file: a `_comment` array + `predicates[]` with no `mode` on any entry. Same rejection mode as demo-sync if a user copies the array.)
    - src/DynamicWeb.Serializer/Configuration/full-sync-example.json (current ~1900-line file with a top-level `conflictStrategy` + flat `predicates[]` listing virtually every DW table; same legacy shape, same rejection.)
  </read_first>
  <action>
1. **demo-sync.json** — Rewrite the file:
   - Remove the top-level `"conflictStrategy": "source-wins",` key (line 5 in current file). Per D-02, conflictStrategy is hardcoded per mode in SerializerConfiguration and not exposed as a top-level config knob; including it would be silently ignored by Plan 01's loader (the post-Plan-01 RawSerializerConfiguration doesn't declare a top-level `ConflictStrategy` field, so System.Text.Json drops unknown keys by default — but leaving them in the example confuses users into thinking they have meaning).
   - Add `"mode": "Deploy"` as the second key (after `"name"`) on every predicate entry. The demo-sync file is conceptually a Deploy-mode example (it ships static reference data).
   - Preserve every other field on every predicate verbatim: `providerType`, `areaId`, `path`, `excludes`, `table`, `serviceCaches`, etc. Do NOT trim or reorder.
   - Result: top-level keys are `outputDirectory`, `logLevel`, `dryRun`, `predicates`. Every predicate entry has `name`, `mode`, then the existing fields.

   For example, the first two predicates rewrite from:
   ```json
   {
     "name": "Content - Swift 2",
     "providerType": "Content",
     "areaId": 3,
     "path": "/",
     "excludes": ["/Home"]
   },
   {
     "name": "EcomCountries",
     "providerType": "SqlTable",
     "table": "EcomCountries",
     "serviceCaches": [
       "Dynamicweb.Ecommerce.International.CountryRelationService",
       "Dynamicweb.Ecommerce.International.CountryService"
     ]
   }
   ```
   to:
   ```json
   {
     "name": "Content - Swift 2",
     "mode": "Deploy",
     "providerType": "Content",
     "areaId": 3,
     "path": "/",
     "excludes": ["/Home"]
   },
   {
     "name": "EcomCountries",
     "mode": "Deploy",
     "providerType": "SqlTable",
     "table": "EcomCountries",
     "serviceCaches": [
       "Dynamicweb.Ecommerce.International.CountryRelationService",
       "Dynamicweb.Ecommerce.International.CountryService"
     ]
   }
   ```
   The same pattern applies to all 19 predicate entries in the file.

2. **ecommerce-predicates-example.json** — Rewrite the file:
   - The `_comment` array at the top stays (purely documentation).
   - Add `"mode": "Deploy"` as the second key on every predicate entry. This file ships fragments meant to be copy-pasted into a main config; Deploy is the safer default for ecommerce settings tables (which is what the file documents).
   - Preserve every other field. Note: every predicate currently has both `nameColumn` and `compareColumns` fields (often empty strings); those stay.
   - Update the `_comment` array to add a final entry: `"Phase 40: each predicate must declare 'mode' (Deploy or Seed). All entries below are tagged 'Deploy' — change to 'Seed' for tables that should use field-level merge per Phase 39."`

3. **full-sync-example.json** — Rewrite the file:
   - Remove the top-level `"conflictStrategy": "source-wins",` key (line 5).
   - Add `"mode": "Deploy"` as the second key on every predicate entry. This file is the "kitchen sink" example that lists ~150 DW tables; Deploy is the only sensible default at this scale (Seed would imply field-level merge across the entire DB, which is not what the example illustrates).
   - Preserve the trailing top-level keys `serializeRoot`, `uploadDir`, `downloadDir`, `logDir` (lines 1895-1898 in current file).
   - Note: many predicates have explicit `null` values on optional fields (`nameColumn: null`, `compareColumns: null`, etc.). Keep them as-is — they parse the same.
   - Volume: ~150 predicates. The pattern is mechanical: for each `{ "name": "X", "providerType": "SqlTable", ... }` block, insert `"mode": "Deploy",` between `"name"` and `"providerType"`. The two Content predicates near the bottom (lines 1869, 1882) get the same treatment.

4. After all three files are rewritten, smoke-test each via `ConfigLoader.Load`:
   ```bash
   # ad-hoc sanity check — these files don't have associated tests, but a load against
   # ConfigLoader.Load(path, identifierValidator: null) would parse without throwing.
   ```
   Add a NEW test in `tests/DynamicWeb.Serializer.Tests/Configuration/ExampleConfigsLoadTests.cs` (one fact per file):
   ```csharp
   [Fact]
   public void Load_DemoSync_Parses()
   {
       var path = Path.Combine(AppContext.BaseDirectory,
           "..", "..", "..", "..", "..", "src", "DynamicWeb.Serializer", "Configuration", "demo-sync.json");
       var config = ConfigLoader.Load(Path.GetFullPath(path), identifierValidator: null);
       Assert.NotEmpty(config.Predicates);
       Assert.All(config.Predicates, p => Assert.Equal(DeploymentMode.Deploy, p.Mode));
   }

   [Fact]
   public void Load_EcommercePredicatesExample_Parses() { /* same pattern, ecommerce-predicates-example.json */ }

   [Fact]
   public void Load_FullSyncExample_Parses() { /* same pattern, full-sync-example.json */ }
   ```
   The `Assert.All` check guards against a future regression where someone forgets to tag a single predicate with `mode` and ConfigLoader would silently default it (it doesn't — Plan 01 makes `mode` required and rejects missing entries, so the `Assert.NotEmpty` path proves every predicate parsed with a Mode).
  </action>
  <verify>
    <automated>cd C:/VibeCode/DynamicWeb.Serializer &amp;&amp; for f in src/DynamicWeb.Serializer/Configuration/demo-sync.json src/DynamicWeb.Serializer/Configuration/ecommerce-predicates-example.json src/DynamicWeb.Serializer/Configuration/full-sync-example.json; do MODE_COUNT=$(grep -c "\"mode\":" $f); NAME_COUNT=$(grep -c "\"name\":" $f); if [ "$MODE_COUNT" -ne "$NAME_COUNT" ]; then echo "FAIL: $f mode($MODE_COUNT) != name($NAME_COUNT)"; else echo "PASS: $f $MODE_COUNT/$NAME_COUNT"; fi; done &amp;&amp; ! grep -E '^\s*"conflictStrategy"' src/DynamicWeb.Serializer/Configuration/demo-sync.json src/DynamicWeb.Serializer/Configuration/full-sync-example.json &amp;&amp; dotnet test tests/DynamicWeb.Serializer.Tests/DynamicWeb.Serializer.Tests.csproj --filter "FullyQualifiedName~ExampleConfigsLoadTests" --no-restore 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `grep -c "\"mode\":" src/DynamicWeb.Serializer/Configuration/demo-sync.json` equals `grep -c "\"name\":" src/DynamicWeb.Serializer/Configuration/demo-sync.json` (one mode per named predicate)
    - Same equality holds for ecommerce-predicates-example.json and full-sync-example.json
    - `grep -c "\"conflictStrategy\"" src/DynamicWeb.Serializer/Configuration/demo-sync.json` returns 0
    - `grep -c "\"conflictStrategy\"" src/DynamicWeb.Serializer/Configuration/full-sync-example.json` returns 0
    - All three new ExampleConfigsLoadTests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Swift2.2-baseline.md and configuration.md docs</name>
  <read_first>
    - docs/baselines/Swift2.2-baseline.md (full file — find every reference to "deploy" / "seed" / "section" / "three-bucket" wording)
    - docs/configuration.md (full file — likely has the legacy shape examples)
  </read_first>
  <action>
1. **docs/baselines/Swift2.2-baseline.md** — make the following edits:
   - Find every mention of "the deploy section" / "deploy.predicates[]" / "seed.predicates[]" and rewrite to reference the flat shape with per-predicate `mode`.
   - Find the "three-bucket split" section header (around line 32-36 per the grep output): the buckets DEPLOYMENT / SEED / NOT-SERIALIZED are conceptually preserved; only the JSON structure that expresses them changes. Add a paragraph after the bucket table:
     ```markdown
     **Phase 40 update (2026-04-28):** The DEPLOYMENT and SEED buckets are now expressed via a single
     flat `predicates: [...]` list at the top of the config, where each predicate carries its own
     `"mode": "Deploy"` or `"mode": "Seed"` field. The legacy `deploy: { predicates: [...] }` /
     `seed: { predicates: [...] }` section shape is rejected by ConfigLoader with a clear error
     pointing here. NOT-SERIALIZED items remain absent from the config (out of scope by definition).
     ```
   - Add a new short subsection after the bucket section, titled `## Per-predicate mode`:
     ```markdown
     ## Per-predicate mode

     Every predicate in `swift2.2-combined.json` declares its own `mode`. Predicates with
     `"mode": "Deploy"` run during Deploy operations (source-wins; YAML overwrites target on every
     deploy). Predicates with `"mode": "Seed"` run during Seed operations (field-level merge per
     Phase 39; YAML fills only fields the target has not set, preserving customer edits across
     re-deploys).

     The mode is set when a predicate is created in the admin UI (Settings → System → Developer →
     Serialize → Predicates → New Predicate). The Mode field on the predicate edit screen is a
     two-option Select. Existing predicates can be re-modded by editing the predicate; there is no
     delete-and-recreate dance. The `swift2.2-combined.json` baseline ships with 17 Deploy and 9
     Seed predicates (counts may shift as the baseline evolves — check the file directly).
     ```
   - Search for any remaining literal `"deploy": {` / `"seed": {` JSON examples in the doc body and rewrite each to the flat shape.

2. **docs/configuration.md** — update every JSON example to the flat shape. Specifically:
   - Replace any example showing
     ```json
     {
       "outputDirectory": "...",
       "deploy": {
         "predicates": [...]
       },
       "seed": {
         "predicates": [...]
       }
     }
     ```
     with
     ```json
     {
       "outputDirectory": "...",
       "deployOutputSubfolder": "deploy",
       "seedOutputSubfolder": "seed",
       "predicates": [
         { "name": "...", "mode": "Deploy", "providerType": "Content", "areaId": 3, "path": "/" },
         { "name": "...", "mode": "Seed", "providerType": "SqlTable", "table": "EcomGroups" }
       ]
     }
     ```
   - Add a `## Per-predicate mode` subsection if the doc doesn't already have one — same wording as the parallel section in Swift2.2-baseline.md (link the two if appropriate).
   - Note: if `docs/configuration.md` does not currently exist, skip its update — `Glob` for it first; only update existing files. The verify step will confirm.

3. The third memory file `project_baseline_workflow.md` and `project_phase39_seed_merge.md` are mentioned in the orchestrator brief as having Phase-40-relevant content — those are USER memory files at `C:\Users\justi\.claude\projects\...\memory\`. **Do NOT modify user memory files** — they are user-curated. The project Memory file `feedback_no_backcompat.md` (also user-managed) confirms the no-backcompat policy this plan honors.
  </action>
  <verify>
    <automated>cd C:/VibeCode/DynamicWeb.Serializer &amp;&amp; grep -E "deploy\.predicates\[|seed\.predicates\[" docs/baselines/Swift2.2-baseline.md docs/configuration.md 2&gt;/dev/null &amp;&amp; echo FAIL_legacy_shape_still_referenced || echo PASS_no_legacy_section_refs</automated>
    Expected: PASS_no_legacy_section_refs
  </verify>
  <done>
    - `grep -E "deploy\.predicates\[|seed\.predicates\[" docs/baselines/Swift2.2-baseline.md docs/configuration.md` returns no matches
    - `grep -c "Phase 40\|per-predicate mode" docs/baselines/Swift2.2-baseline.md` returns at least 2 (one in the Phase 40 update paragraph, one in the new subsection)
    - At least one `"mode":` example appears in each of the two updated docs
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live load of new baseline against Swift 2.2 host</name>
  <what-built>
    - swift2.2-combined.json rewritten in flat shape (Plan 04 Task 1)
    - demo-sync.json, ecommerce-predicates-example.json, full-sync-example.json rewritten in flat shape (Plan 04 Task 2)
    - Two reasoning docs updated (Plan 04 Task 3)
    - All compile + unit tests passing (Plans 01, 02, 03 prerequisites already gated)
  </what-built>
  <how-to-verify>
1. Confirm Swift 2.2 host is running on port 54035 (project memory `reference_dw_hosts.md` for credentials). If not running, start it.

2. Copy the new baseline into the Swift 2.2 host's Files folder, replacing the existing config:
   ```powershell
   Copy-Item C:/VibeCode/DynamicWeb.Serializer/src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json `
             C:/path/to/swift22-host/wwwroot/Files/Serializer.config.json -Force
   ```
   (User: substitute the actual swift22 host path you set up in `reference_cleandb_host_startup.md`.)

3. Open the admin UI: `https://localhost:54035/Admin/`. Navigate `Settings → System → Developer → Serialize`. Confirm:
   - The tree shows exactly 4 children: `Predicates`, `Item Types`, `Embedded XML`, `Log Viewer` (no `Deploy` / `Seed` group nodes)
   - Click `Predicates` → list shows 26 rows; the first column on each row reads `Deploy` or `Seed`
   - Click any predicate row → the edit screen opens; the `Mode` field is visible and editable as a Select with `Deploy` / `Seed` options

4. Run a test serialize via the admin UI action: `Settings → System → Developer → Serialize → Serialize Deploy` (or whatever the trigger is on your local). Expected: completes without HTTP 5xx; log shows the 17 Deploy predicates iterated.

5. Run a test deserialize via API curl from PowerShell:
   ```powershell
   $cred = Get-Credential   # use admin creds from reference_dw_hosts.md
   Invoke-WebRequest -Uri "https://localhost:54035/Admin/Api/SerializerSerialize?mode=seed" `
                     -Method POST -Credential $cred -SkipCertificateCheck
   ```
   Expected: HTTP 200; the response body's "Predicates: 9" matches the seed-predicate count.

6. Test the legacy-rejection path. Briefly rename your `Serializer.config.json` to `Serializer.config.legacy.json`, restore the OLD `swift2.2-combined.json` from git history (`git show HEAD~3:src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json > /tmp/legacy.json`), copy it as `Serializer.config.json`, then trigger any serializer command. Expected: the command returns Error with message containing "Legacy section-level shape" and "Phase 40". Then restore the new baseline.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any failure (e.g., "tree shows 5 children", "predicate edit screen missing Mode field", "HTTP 500 on Deploy serialize", or "legacy rejection didn't fire").</resume-signal>
  <action>This is a human-verify checkpoint — the executor pauses here. The verification steps the operator runs are enumerated in &lt;how-to-verify&gt; above. No automated action is required from the executor; resume on operator signal.</action>
  <verify>
    <automated>echo "Manual checkpoint — verification deferred to operator. Resume signal expected."</automated>
  </verify>
  <done>Operator types "approved" OR enumerates a specific failure mode that routes the phase back to the planner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| swift2.2-combined.json on disk → ConfigLoader | Authored content; if shape is wrong, every consumer breaks |
| Live host file copy → admin UI render | Operator-driven file replacement |
| Example JSONs (demo-sync, ecommerce-predicates-example, full-sync-example) → user copy/paste | If shape is legacy, user-copied config is hard-rejected at next load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04-01 | Tampering | Hand-written JSON has typo in `mode` value | mitigate | Plan 01's ConfigLoader rejects unparseable `mode` values with a clear error citing the predicate name. Task 1's `Swift22BaselineRoundTripTests` exercises the full file end-to-end and asserts both Deploy and Seed counts. Task 2's `ExampleConfigsLoadTests` mirrors the protection across the three example files. Any typo (e.g., `"mode": "deplooy"`) fails both tests. |
| T-40-04-02 | Information disclosure | Doc examples ship credentials or internal-only details | accept | Both docs already exist with no credentials. The Phase 40 edit only changes config-shape examples; no new sensitive content is added. |
| T-40-04-03 | DoS | Live host fails to load new baseline because of an unrelated bug | mitigate | Task 4 (the human checkpoint) explicitly verifies the live host loads the new baseline AND that the legacy-rejection error fires. Failure modes are enumerated in `<resume-signal>` so the operator can describe specifically which path broke. |
| T-40-04-04 | Tampering | User copies an example JSON, ConfigLoader rejects it for missing `mode`, user loses confidence | mitigate | Task 2 rewrites all three example JSONs into the new shape with explicit `"mode": "Deploy"` on every predicate. ExampleConfigsLoadTests provides regression coverage so a future contributor cannot silently regress one of the example files into a state ConfigLoader would reject. |
</threat_model>

<verification>
After Tasks 1 + 2 + 3 (the auto tasks) complete, before opening the human-verify checkpoint:
```
cd C:/VibeCode/DynamicWeb.Serializer
dotnet build src/DynamicWeb.Serializer/DynamicWeb.Serializer.csproj 2>&1 | grep -c "error CS"  # should be 0
dotnet test tests/DynamicWeb.Serializer.Tests/DynamicWeb.Serializer.Tests.csproj --no-restore  # all green
grep -E "deploy\.predicates\[|seed\.predicates\[" docs/baselines/Swift2.2-baseline.md docs/configuration.md  # zero matches
grep -E '^\s*"deploy":\s*\{|^\s*"seed":\s*\{' src/DynamicWeb.Serializer/Configuration/swift2.2-combined.json  # zero matches
grep -E '^\s*"conflictStrategy"' src/DynamicWeb.Serializer/Configuration/demo-sync.json src/DynamicWeb.Serializer/Configuration/full-sync-example.json  # zero matches
for f in src/DynamicWeb.Serializer/Configuration/demo-sync.json src/DynamicWeb.Serializer/Configuration/ecommerce-predicates-example.json src/DynamicWeb.Serializer/Configuration/full-sync-example.json; do test "$(grep -c '"mode":' $f)" = "$(grep -c '"name":' $f)" || echo "FAIL: $f"; done  # zero FAIL lines
```
All checks must pass before the checkpoint goes live.

Task 4 (human-verify) is the gate that closes the phase. The operator has full authority to reject and request a fix.

Note on plan-level autonomy: per checker Blocker #4, the frontmatter declares `autonomous: true` even though Task 4 is a `checkpoint:human-verify`. The pattern matches Phases 37/38/39 — the executor relies on the per-task `<task type="checkpoint:human-verify" gate="blocking">` element to drive the human gate, not on the plan-level `autonomous: false` flag. Setting `autonomous: false` would have halted the executor at Task 1 (per checker Blocker #4), which is wrong — Tasks 1-3 are fully autonomous and should run to completion before the operator is asked to validate Task 4.
</verification>

<success_criteria>
- swift2.2-combined.json is in the flat shape — single `predicates` array, every entry has `mode`, no `deploy`/`seed` section keys
- demo-sync.json, ecommerce-predicates-example.json, full-sync-example.json are in the flat shape with `"mode": "Deploy"` on every predicate; no top-level `conflictStrategy` key
- Swift22BaselineRoundTripTests + ExampleConfigsLoadTests pass
- docs/baselines/Swift2.2-baseline.md and docs/configuration.md describe the new shape with at least one example each
- Operator confirms the live Swift 2.2 host loads the new baseline cleanly AND that the legacy-shape rejection error fires when the legacy file is loaded
</success_criteria>

<output>
After completion, create `.planning/phases/40-per-predicate-deploy-seed-split-replace-section-level-config/40-04-SUMMARY.md` with:
- Final predicate count + mode distribution in the new baseline (should be 17 Deploy + 9 Seed = 26 total)
- Disposition of each example JSON: predicate count, all-Deploy default rationale
- Whether the multi-line vs. compact formatting was kept (and why)
- Operator's checkpoint disposition (approved / fixes / disposition narrative)
- Any docs that were also updated beyond the two listed (note them so the next phase's history-digest captures them)
- Closing note for the phase as a whole — Plan 05 is the gate-of-gates final check, this plan is the data + docs deliverable
</output>
