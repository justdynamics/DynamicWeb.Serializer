---
phase: 19-source-id-serialization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/DynamicWeb.Serializer/Models/SerializedPage.cs
  - src/DynamicWeb.Serializer/Models/SerializedParagraph.cs
  - src/DynamicWeb.Serializer/Serialization/ContentMapper.cs
  - tests/DynamicWeb.Serializer.Tests/Models/DtoTests.cs
  - tests/DynamicWeb.Serializer.Tests/Infrastructure/YamlRoundTripTests.cs
  - tests/DynamicWeb.Serializer.Tests/Fixtures/ContentTreeBuilder.cs
autonomous: true
requirements:
  - SER-01
  - SER-02

must_haves:
  truths:
    - "SerializedPage YAML files contain a SourcePageId field with the numeric page ID from the source environment"
    - "SerializedParagraph YAML files contain a SourceParagraphId field with the numeric paragraph ID from the source environment"
    - "Existing deserialization still works correctly with the new fields present (backward compatible)"
    - "SourcePageId and SourceParagraphId are nullable so YAML files without them still deserialize"
  artifacts:
    - path: "src/DynamicWeb.Serializer/Models/SerializedPage.cs"
      provides: "SourcePageId property on SerializedPage record"
      contains: "SourcePageId"
    - path: "src/DynamicWeb.Serializer/Models/SerializedParagraph.cs"
      provides: "SourceParagraphId property on SerializedParagraph record"
      contains: "SourceParagraphId"
    - path: "src/DynamicWeb.Serializer/Serialization/ContentMapper.cs"
      provides: "Populates SourcePageId from page.ID and SourceParagraphId from paragraph.ID"
      contains: "SourcePageId"
    - path: "tests/DynamicWeb.Serializer.Tests/Models/DtoTests.cs"
      provides: "Unit tests for SourcePageId and SourceParagraphId"
      contains: "SourcePageId"
    - path: "tests/DynamicWeb.Serializer.Tests/Infrastructure/YamlRoundTripTests.cs"
      provides: "YAML round-trip test for SourcePageId and SourceParagraphId"
      contains: "SourcePageId"
  key_links:
    - from: "src/DynamicWeb.Serializer/Serialization/ContentMapper.cs"
      to: "src/DynamicWeb.Serializer/Models/SerializedPage.cs"
      via: "MapPage sets SourcePageId = page.ID"
      pattern: "SourcePageId\\s*=\\s*page\\.ID"
    - from: "src/DynamicWeb.Serializer/Serialization/ContentMapper.cs"
      to: "src/DynamicWeb.Serializer/Models/SerializedParagraph.cs"
      via: "MapParagraph sets SourceParagraphId = paragraph.ID"
      pattern: "SourceParagraphId\\s*=\\s*paragraph\\.ID"
---

<objective>
Add source numeric IDs to serialized YAML so deserialization can build source-to-target ID mappings for internal link resolution.

Purpose: Phase 20 (Link Resolution Core) needs to know what numeric page ID each page had in the source environment. By serializing SourcePageId and SourceParagraphId into YAML alongside the existing GUIDs, the deserializer can build a { sourcePageId -> targetPageId } map using GUID as the bridge.

Output: Updated SerializedPage and SerializedParagraph DTOs with source ID fields, updated ContentMapper to populate them, unit tests confirming round-trip and backward compatibility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md

@src/DynamicWeb.Serializer/Models/SerializedPage.cs
@src/DynamicWeb.Serializer/Models/SerializedParagraph.cs
@src/DynamicWeb.Serializer/Serialization/ContentMapper.cs
@tests/DynamicWeb.Serializer.Tests/Models/DtoTests.cs
@tests/DynamicWeb.Serializer.Tests/Infrastructure/YamlRoundTripTests.cs
@tests/DynamicWeb.Serializer.Tests/Fixtures/ContentTreeBuilder.cs

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/DynamicWeb.Serializer/Models/SerializedPage.cs:
```csharp
public record SerializedPage
{
    public required Guid PageUniqueId { get; init; }
    public required string Name { get; init; }
    public required string MenuText { get; init; }
    public required string UrlName { get; init; }
    public required int SortOrder { get; init; }
    // ... other fields ...
    public Dictionary<string, object> Fields { get; init; } = new();
    public List<SerializedPage> Children { get; init; } = new();
}
```

From src/DynamicWeb.Serializer/Models/SerializedParagraph.cs:
```csharp
public record SerializedParagraph
{
    public required Guid ParagraphUniqueId { get; init; }
    public required int SortOrder { get; init; }
    // ... other fields ...
    public Dictionary<string, object> Fields { get; init; } = new();
    public int? ColumnId { get; init; }
}
```

From src/DynamicWeb.Serializer/Serialization/ContentMapper.cs:
```csharp
public SerializedPage MapPage(Page page, List<SerializedGridRow> gridRows, List<SerializedPage> children, List<SerializedPermission> permissions)
public SerializedParagraph MapParagraph(Paragraph paragraph)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SourcePageId and SourceParagraphId to DTOs and ContentMapper</name>
  <files>
    src/DynamicWeb.Serializer/Models/SerializedPage.cs,
    src/DynamicWeb.Serializer/Models/SerializedParagraph.cs,
    src/DynamicWeb.Serializer/Serialization/ContentMapper.cs
  </files>
  <read_first>
    src/DynamicWeb.Serializer/Models/SerializedPage.cs,
    src/DynamicWeb.Serializer/Models/SerializedParagraph.cs,
    src/DynamicWeb.Serializer/Serialization/ContentMapper.cs
  </read_first>
  <behavior>
    - SerializedPage has a nullable int property SourcePageId (defaults to null for backward compat)
    - SerializedParagraph has a nullable int property SourceParagraphId (defaults to null for backward compat)
    - ContentMapper.MapPage sets SourcePageId = page.ID (the DW Page numeric ID)
    - ContentMapper.MapParagraph sets SourceParagraphId = paragraph.ID (the DW Paragraph numeric ID)
    - Existing YAML without SourcePageId/SourceParagraphId still deserializes (null default)
  </behavior>
  <action>
    1. In SerializedPage.cs, add after the existing `PageUniqueId` property:
       `public int? SourcePageId { get; init; }`
       This is nullable (no `required`) so existing YAML files without this field still deserialize correctly.

    2. In SerializedParagraph.cs, add after the existing `ParagraphUniqueId` property:
       `public int? SourceParagraphId { get; init; }`
       Same rationale -- nullable for backward compatibility.

    3. In ContentMapper.cs MapPage method, add to the SerializedPage constructor:
       `SourcePageId = page.ID,`
       The DW `Page.ID` property is the numeric primary key in the source database.

    4. In ContentMapper.cs MapParagraph method, add to the SerializedParagraph constructor:
       `SourceParagraphId = paragraph.ID,`
       The DW `Paragraph.ID` property is the numeric primary key in the source database.

    Per SER-01: SerializedPage DTO includes SourcePageId alongside existing GUID.
    Per SER-02: SerializedParagraph DTO includes SourceParagraphId for paragraph anchor resolution.
  </action>
  <verify>
    <automated>cd C:/VibeCode/DynamicWeb.Serializer &amp;&amp; dotnet build src/DynamicWeb.Serializer/DynamicWeb.Serializer.csproj --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "SourcePageId" src/DynamicWeb.Serializer/Models/SerializedPage.cs returns 1
    - grep -c "SourceParagraphId" src/DynamicWeb.Serializer/Models/SerializedParagraph.cs returns 1
    - grep "SourcePageId = page.ID" src/DynamicWeb.Serializer/Serialization/ContentMapper.cs returns a match
    - grep "SourceParagraphId = paragraph.ID" src/DynamicWeb.Serializer/Serialization/ContentMapper.cs returns a match
    - grep "int?" src/DynamicWeb.Serializer/Models/SerializedPage.cs shows SourcePageId is nullable
    - grep "int?" src/DynamicWeb.Serializer/Models/SerializedParagraph.cs shows SourceParagraphId is nullable
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>SerializedPage has SourcePageId (int?), SerializedParagraph has SourceParagraphId (int?), ContentMapper populates both from DW numeric IDs, project compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unit tests for source ID fields and YAML round-trip</name>
  <files>
    tests/DynamicWeb.Serializer.Tests/Models/DtoTests.cs,
    tests/DynamicWeb.Serializer.Tests/Infrastructure/YamlRoundTripTests.cs,
    tests/DynamicWeb.Serializer.Tests/Fixtures/ContentTreeBuilder.cs
  </files>
  <read_first>
    tests/DynamicWeb.Serializer.Tests/Models/DtoTests.cs,
    tests/DynamicWeb.Serializer.Tests/Infrastructure/YamlRoundTripTests.cs,
    tests/DynamicWeb.Serializer.Tests/Fixtures/ContentTreeBuilder.cs,
    src/DynamicWeb.Serializer/Models/SerializedPage.cs,
    src/DynamicWeb.Serializer/Models/SerializedParagraph.cs
  </read_first>
  <behavior>
    - Test: SerializedPage with SourcePageId=42 round-trips through YAML and comes back as 42
    - Test: SerializedParagraph with SourceParagraphId=99 round-trips through YAML and comes back as 99
    - Test: SerializedPage without SourcePageId deserializes to null (backward compat)
    - Test: SerializedParagraph without SourceParagraphId deserializes to null (backward compat)
    - Test: SourcePageId defaults to null when constructed without it
    - Test: SourceParagraphId defaults to null when constructed without it
    - ContentTreeBuilder helper methods updated to optionally include source IDs
  </behavior>
  <action>
    1. In DtoTests.cs, add these tests:

       a. `SerializedPage_SourcePageId_DefaultsToNull` -- construct SerializedPage with required fields only, assert SourcePageId is null.

       b. `SerializedPage_SourcePageId_CanBeSet` -- construct SerializedPage with SourcePageId = 42, assert it equals 42.

       c. `SerializedParagraph_SourceParagraphId_DefaultsToNull` -- construct SerializedParagraph with required fields only, assert SourceParagraphId is null.

       d. `SerializedParagraph_SourceParagraphId_CanBeSet` -- construct SerializedParagraph with SourceParagraphId = 99, assert it equals 99.

    2. In YamlRoundTripTests.cs, add these tests:

       a. `Yaml_RoundTrips_PageWithSourcePageId` -- create SerializedPage with SourcePageId = 42, serialize to YAML, deserialize back, assert SourcePageId == 42.

       b. `Yaml_RoundTrips_ParagraphWithSourceParagraphId` -- create SerializedParagraph with SourceParagraphId = 99, serialize to YAML, deserialize back, assert SourceParagraphId == 99.

       c. `Yaml_Deserializes_PageWithoutSourcePageId_AsNull` -- serialize a page without SourcePageId (the default), deserialize, assert SourcePageId is null. This proves backward compatibility with existing YAML files.

       d. `Yaml_Deserializes_ParagraphWithoutSourceParagraphId_AsNull` -- same pattern for paragraph.

    3. In ContentTreeBuilder.cs, add a new helper method:

       ```csharp
       public static SerializedPage BuildSinglePageWithSourceId(string name, int sourcePageId, Guid? guid = null)
       {
           return BuildSinglePage(name, guid) with { SourcePageId = sourcePageId };
       }
       ```

    4. Run ALL existing tests to ensure nothing is broken (backward compatibility).
  </action>
  <verify>
    <automated>cd C:/VibeCode/DynamicWeb.Serializer &amp;&amp; dotnet test tests/DynamicWeb.Serializer.Tests/DynamicWeb.Serializer.Tests.csproj --no-restore --verbosity normal 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "SourcePageId_DefaultsToNull" tests/DynamicWeb.Serializer.Tests/Models/DtoTests.cs returns a match
    - grep "SourceParagraphId_DefaultsToNull" tests/DynamicWeb.Serializer.Tests/Models/DtoTests.cs returns a match
    - grep "Yaml_RoundTrips_PageWithSourcePageId" tests/DynamicWeb.Serializer.Tests/Infrastructure/YamlRoundTripTests.cs returns a match
    - grep "Yaml_RoundTrips_ParagraphWithSourceParagraphId" tests/DynamicWeb.Serializer.Tests/Infrastructure/YamlRoundTripTests.cs returns a match
    - grep "Yaml_Deserializes_PageWithoutSourcePageId_AsNull" tests/DynamicWeb.Serializer.Tests/Infrastructure/YamlRoundTripTests.cs returns a match
    - grep "BuildSinglePageWithSourceId" tests/DynamicWeb.Serializer.Tests/Fixtures/ContentTreeBuilder.cs returns a match
    - dotnet test passes with 0 failures
    - All pre-existing tests still pass (no regressions)
  </acceptance_criteria>
  <done>8 new unit tests pass (4 DTO tests + 4 YAML round-trip tests), all existing tests pass, ContentTreeBuilder has source ID helper method.</done>
</task>

</tasks>

<verification>
1. `dotnet build` succeeds with 0 errors
2. `dotnet test` passes all tests (new + existing)
3. SerializedPage.cs contains `public int? SourcePageId { get; init; }`
4. SerializedParagraph.cs contains `public int? SourceParagraphId { get; init; }`
5. ContentMapper.MapPage sets `SourcePageId = page.ID`
6. ContentMapper.MapParagraph sets `SourceParagraphId = paragraph.ID`
7. YAML round-trip preserves SourcePageId and SourceParagraphId values
8. YAML without source IDs still deserializes (null defaults = backward compat)
</verification>

<success_criteria>
- SerializedPage YAML includes SourcePageId with the source environment's numeric page ID
- SerializedParagraph YAML includes SourceParagraphId with the source environment's numeric paragraph ID
- Existing YAML without these fields still deserializes correctly (nullable = backward compatible)
- All unit tests pass including new source ID tests
- Phase 20 can consume SourcePageId from YAML to build source-to-target ID mapping
</success_criteria>

<output>
After completion, create `.planning/phases/19-source-id-serialization/19-01-SUMMARY.md`
</output>
