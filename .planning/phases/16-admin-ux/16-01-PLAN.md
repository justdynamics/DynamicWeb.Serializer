---
phase: 16-admin-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dynamicweb.ContentSync.sln
  - src/Dynamicweb.ContentSync/Dynamicweb.ContentSync.csproj
  - tests/Dynamicweb.ContentSync.Tests/Dynamicweb.ContentSync.Tests.csproj
  - tests/Dynamicweb.ContentSync.IntegrationTests/Dynamicweb.ContentSync.IntegrationTests.csproj
  - "All .cs files in src/ and tests/ (namespace + using updates)"
  - README.md
autonomous: true
requirements: [REN-01]

must_haves:
  truths:
    - "Root namespace is DynamicWeb.Serializer across all source files"
    - "Assembly output is DynamicWeb.Serializer.dll"
    - "NuGet package ID is DynamicWeb.Serializer"
    - "API command classes are SerializerSerializeCommand and SerializerDeserializeCommand"
    - "Config file lookup checks Serializer.config.json first, ContentSync.config.json as fallback"
    - "Solution builds and all unit tests pass under new names"
  artifacts:
    - path: "DynamicWeb.Serializer.sln"
      provides: "Renamed solution file"
    - path: "src/DynamicWeb.Serializer/DynamicWeb.Serializer.csproj"
      provides: "Renamed project with updated assembly/package metadata"
    - path: "tests/DynamicWeb.Serializer.Tests/DynamicWeb.Serializer.Tests.csproj"
      provides: "Renamed test project"
  key_links:
    - from: "tests/DynamicWeb.Serializer.Tests/*.csproj"
      to: "src/DynamicWeb.Serializer/DynamicWeb.Serializer.csproj"
      via: "ProjectReference"
      pattern: "DynamicWeb\\.Serializer\\.csproj"
    - from: "ConfigPathResolver.cs"
      to: "Serializer.config.json"
      via: "CandidatePaths array"
      pattern: "Serializer\\.config\\.json"
---

<objective>
Full project rename from Dynamicweb.ContentSync to DynamicWeb.Serializer (per D-01 through D-05).

Purpose: The project has expanded beyond content sync to full database serialization. The rename reflects this expanded scope and must land before any Wave 2 UX work to avoid double-touching files.
Output: All source files, project files, solution file, and README updated to DynamicWeb.Serializer identity. Build green, tests green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-admin-ux/16-CONTEXT.md
@.planning/phases/16-admin-ux/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename solution, projects, directories, and all namespace/using references</name>
  <files>
    DynamicWeb.Serializer.sln (renamed from Dynamicweb.ContentSync.sln),
    src/DynamicWeb.Serializer/DynamicWeb.Serializer.csproj (renamed from src/Dynamicweb.ContentSync/Dynamicweb.ContentSync.csproj),
    tests/DynamicWeb.Serializer.Tests/DynamicWeb.Serializer.Tests.csproj,
    tests/DynamicWeb.Serializer.IntegrationTests/DynamicWeb.Serializer.IntegrationTests.csproj,
    All .cs files in src/ and tests/
  </files>
  <read_first>
    src/Dynamicweb.ContentSync/Dynamicweb.ContentSync.csproj,
    tests/Dynamicweb.ContentSync.Tests/Dynamicweb.ContentSync.Tests.csproj,
    tests/Dynamicweb.ContentSync.IntegrationTests/Dynamicweb.ContentSync.IntegrationTests.csproj,
    Dynamicweb.ContentSync.sln,
    src/Dynamicweb.ContentSync/Configuration/ConfigPathResolver.cs,
    src/Dynamicweb.ContentSync/AdminUI/Commands/ContentSyncSerializeCommand.cs,
    src/Dynamicweb.ContentSync/AdminUI/Commands/ContentSyncDeserializeCommand.cs,
    src/Dynamicweb.ContentSync/AdminUI/Injectors/ContentSyncPageListInjector.cs,
    README.md
  </read_first>
  <action>
    This is a mechanical but wide-reaching rename. Execute in this exact order:

    **Step 1: Rename directories (two-step for Windows case sensitivity per Pitfall 2)**
    Use `git mv` to rename directories. Because the casing changes (lowercase `w` to uppercase `W`), do a two-step rename through a temp name:
    - `git mv src/Dynamicweb.ContentSync src/DW-Serializer-temp` then `git mv src/DW-Serializer-temp src/DynamicWeb.Serializer`
    - `git mv tests/Dynamicweb.ContentSync.Tests tests/DW-Serializer-Tests-temp` then `git mv tests/DW-Serializer-Tests-temp tests/DynamicWeb.Serializer.Tests`
    - `git mv tests/Dynamicweb.ContentSync.IntegrationTests tests/DW-Serializer-IT-temp` then `git mv tests/DW-Serializer-IT-temp tests/DynamicWeb.Serializer.IntegrationTests`
    - `git mv Dynamicweb.ContentSync.sln DynamicWeb.Serializer.sln`

    **Step 2: Update .csproj files**
    In `src/DynamicWeb.Serializer/DynamicWeb.Serializer.csproj`:
    - `<RootNamespace>DynamicWeb.Serializer</RootNamespace>`
    - `<AssemblyName>DynamicWeb.Serializer</AssemblyName>`
    - `<PackageId>DynamicWeb.Serializer</PackageId>`
    - `<Title>DynamicWeb Serializer</Title>`
    - `<Description>Serialize and deserialize DynamicWeb database state to YAML files for version control and environment sync.</Description>`
    - `<Authors>DynamicWeb Serializer Contributors</Authors>`
    - Update PackageProjectUrl and RepositoryUrl to reference DynamicWeb.Serializer

    In `tests/DynamicWeb.Serializer.Tests/DynamicWeb.Serializer.Tests.csproj`:
    - `<RootNamespace>DynamicWeb.Serializer.Tests</RootNamespace>`
    - Update ProjectReference to `..\..\src\DynamicWeb.Serializer\DynamicWeb.Serializer.csproj`

    In `tests/DynamicWeb.Serializer.IntegrationTests/DynamicWeb.Serializer.IntegrationTests.csproj`:
    - `<RootNamespace>DynamicWeb.Serializer.IntegrationTests</RootNamespace>`
    - Update ProjectReference similarly

    **Step 3: Update .sln file**
    Update all project paths in `DynamicWeb.Serializer.sln` from `Dynamicweb.ContentSync` to `DynamicWeb.Serializer`.

    **Step 4: Rename all namespaces and using statements in .cs files**
    Find and replace across ALL .cs files in src/ and tests/:
    - `namespace Dynamicweb.ContentSync` -> `namespace DynamicWeb.Serializer`
    - `using Dynamicweb.ContentSync` -> `using DynamicWeb.Serializer`
    - `Dynamicweb.ContentSync.Tests` -> `DynamicWeb.Serializer.Tests`
    - `Dynamicweb.ContentSync.IntegrationTests` -> `DynamicWeb.Serializer.IntegrationTests`

    **Step 5: Rename API command classes (per D-04)**
    - Rename file `ContentSyncSerializeCommand.cs` -> `SerializerSerializeCommand.cs`
    - Rename class `ContentSyncSerializeCommand` -> `SerializerSerializeCommand`
    - Rename file `ContentSyncDeserializeCommand.cs` -> `SerializerDeserializeCommand.cs`
    - Rename class `ContentSyncDeserializeCommand` -> `SerializerDeserializeCommand`
    - Rename file `SaveSyncSettingsCommand.cs` -> `SaveSerializerSettingsCommand.cs`
    - Rename class `SaveSyncSettingsCommand` -> `SaveSerializerSettingsCommand`

    **Step 6: Rename other "ContentSync"/"Sync" prefixed classes**
    - `ContentSyncPageEditInjector` -> `SerializerPageEditInjector` (file: `SerializerPageEditInjector.cs`)
    - `ContentSyncPageListInjector` -> `SerializerPageListInjector` (file: `SerializerPageListInjector.cs`) -- note: check if this file exists, research mentions ContentSyncPageListInjector but code shows ContentSyncPageEditInjector. Rename whatever exists.
    - `SyncSettingsNodeProvider` -> `SerializerSettingsNodeProvider` (file: `SerializerSettingsNodeProvider.cs`)
    - `SyncNavigationNodePathProvider` -> `SerializerNavigationNodePathProvider` (file: `SerializerNavigationNodePathProvider.cs`)
    - `SyncSettingsEditScreen` -> `SerializerSettingsEditScreen` (file: `SerializerSettingsEditScreen.cs`)
    - `SyncSettingsModel` -> `SerializerSettingsModel` (file: `SerializerSettingsModel.cs`)
    - `SyncSettingsQuery` -> `SerializerSettingsQuery` (file: `SerializerSettingsQuery.cs`)
    - `SyncConfiguration` -> `SerializerConfiguration` (file: `SerializerConfiguration.cs`) -- if this is the main config model class

    Update ALL references to these renamed classes across the entire codebase (grep for each old name).

    **Step 7: Update ConfigPathResolver for config file backward compat (per Pitfall 4)**
    In `ConfigPathResolver.cs`, update `CandidatePaths` to check `Serializer.config.json` first, then `ContentSync.config.json` as fallback:
    ```
    private static readonly string[] CandidatePaths =
    {
        Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "..", "wwwroot", "Files", "Serializer.config.json"),
        Path.Combine(Directory.GetCurrentDirectory(), "wwwroot", "Files", "Serializer.config.json"),
        Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "Serializer.config.json"),
        Path.Combine(Directory.GetCurrentDirectory(), "Serializer.config.json"),
        // Backward compat: check old name as fallback
        Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "..", "wwwroot", "Files", "ContentSync.config.json"),
        Path.Combine(Directory.GetCurrentDirectory(), "wwwroot", "Files", "ContentSync.config.json"),
        Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "ContentSync.config.json"),
        Path.Combine(Directory.GetCurrentDirectory(), "ContentSync.config.json")
    };
    ```
    Update default output directory in `FindOrCreateConfigFile()`:
    - `OutputDirectory = @"\System\Serializer"` (was `@"\System\ContentSync"`)

    **Step 8: Update string literals referencing "ContentSync"**
    - In command classes: log messages like `"=== ContentSync Serialize"` -> `"=== Serializer Serialize"`
    - Error messages: `"ContentSync.config.json not found"` -> `"Serializer.config.json not found (also checked ContentSync.config.json)"`
    - Screen name: `"Content Sync Settings"` -> `"Serialize Settings"` (per D-10)
    - Page edit injector action group name: `"Content Sync"` -> `"Serialize"`
    - The `ContentSync.log` references in commands will be replaced in Plan 02 with per-run log files

    **Step 9: Update README.md**
    - Title: `# DynamicWeb.Serializer`
    - Replace all `ContentSync` references with `Serializer` / `DynamicWeb.Serializer` as appropriate
    - Update API command examples: `ContentSyncSerialize` -> `SerializerSerialize`, `ContentSyncDeserialize` -> `SerializerDeserialize`
    - Update config file name references: `ContentSync.config.json` -> `Serializer.config.json`
    - Add note about backward compat: existing `ContentSync.config.json` files are still detected as fallback
    - Update tree path: `Settings > Content > Sync` -> `Settings > Database > Serialize`
    - Per D-23 (UX-04): Add section documenting that scheduled tasks are removed and API commands are the replacement. Include the Management API and DW CLI examples that already exist.
    - Add deployment note about removing old `Dynamicweb.ContentSync.dll` from DW instance `bin/` (per Pitfall 3)

    **Step 10: Clean and build**
    - `dotnet clean DynamicWeb.Serializer.sln`
    - `dotnet build DynamicWeb.Serializer.sln`
    - `dotnet test tests/DynamicWeb.Serializer.Tests/ --filter "Category!=Integration"`
  </action>
  <verify>
    <automated>dotnet build DynamicWeb.Serializer.sln && dotnet test tests/DynamicWeb.Serializer.Tests/ --filter "Category!=Integration" -x</automated>
  </verify>
  <acceptance_criteria>
    - No file in src/ or tests/ contains `namespace Dynamicweb.ContentSync` (grep returns 0 results)
    - No file in src/ or tests/ contains `using Dynamicweb.ContentSync` (grep returns 0 results)
    - `src/DynamicWeb.Serializer/DynamicWeb.Serializer.csproj` contains `<RootNamespace>DynamicWeb.Serializer</RootNamespace>`
    - `src/DynamicWeb.Serializer/DynamicWeb.Serializer.csproj` contains `<AssemblyName>DynamicWeb.Serializer</AssemblyName>`
    - `src/DynamicWeb.Serializer/DynamicWeb.Serializer.csproj` contains `<PackageId>DynamicWeb.Serializer</PackageId>`
    - `tests/DynamicWeb.Serializer.Tests/DynamicWeb.Serializer.Tests.csproj` contains `DynamicWeb.Serializer.csproj`
    - File `src/DynamicWeb.Serializer/AdminUI/Commands/SerializerSerializeCommand.cs` exists
    - File `src/DynamicWeb.Serializer/AdminUI/Commands/SerializerDeserializeCommand.cs` exists
    - `ConfigPathResolver.cs` contains `Serializer.config.json`
    - `ConfigPathResolver.cs` contains `ContentSync.config.json` (backward compat fallback)
    - `README.md` contains `# DynamicWeb.Serializer`
    - `README.md` contains `SerializerSerialize` (API command name)
    - `dotnet build DynamicWeb.Serializer.sln` exits 0
    - `dotnet test tests/DynamicWeb.Serializer.Tests/ --filter "Category!=Integration"` exits 0
  </acceptance_criteria>
  <done>All source files use DynamicWeb.Serializer namespace. Assembly is DynamicWeb.Serializer.dll. API commands are SerializerSerialize/SerializerDeserialize. Config lookup checks both new and old filenames. Build and tests green.</done>
</task>

</tasks>

<verification>
- `dotnet build DynamicWeb.Serializer.sln` succeeds
- `dotnet test tests/DynamicWeb.Serializer.Tests/ --filter "Category!=Integration"` passes all tests
- `grep -r "namespace Dynamicweb.ContentSync" src/ tests/` returns no matches
- `grep -r "using Dynamicweb.ContentSync" src/ tests/` returns no matches
</verification>

<success_criteria>
Every file in the repository references DynamicWeb.Serializer (not Dynamicweb.ContentSync). The solution builds, tests pass, and the README reflects the new identity. Config file backward compat is in place.
</success_criteria>

<output>
After completion, create `.planning/phases/16-admin-ux/16-01-SUMMARY.md`
</output>
